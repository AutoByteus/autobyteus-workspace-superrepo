<template><aside class="rounded-lg border border-slate-200 bg-white p-3"><h3 class="text-sm font-semibold text-slate-900">Agent Library</h3><div class="relative mt-2"><input :value="search" type="text" class="block w-full rounded-md border border-slate-300 bg-white py-2 pl-3 pr-2 text-sm" placeholder="Search Agents" @input="$emit('update:search', ($event.target as HTMLInputElement).value)"></div><div class="mt-3 max-h-[26rem] overflow-y-auto pr-1"><p class="text-xs font-semibold uppercase tracking-wide text-slate-500">Agents</p><div class="mt-2 space-y-2"><div v-for="item in agentItems" :key="item.id" draggable="true" class="flex items-center justify-between gap-2 rounded-md border border-slate-200 bg-white px-2 py-2 text-sm" @dragstart="$emit('dragstart-item', { event: $event, item })"><button type="button" class="flex min-w-0 items-center gap-2 text-left" @click="$emit('add', item)"><span class="text-slate-400">⋮⋮</span><span class="truncate font-medium">{{ item.name }}</span></button><span class="rounded-full bg-blue-50 px-2 py-0.5 text-[10px] font-semibold text-blue-700">AGENT</span></div><p v-if="!agentItems.length" class="text-xs text-slate-400">No Agents found.</p></div></div><p class="mt-3 text-xs text-slate-500">Drag Agents into the Team canvas. Teams cannot contain Teams.</p></aside></template>
<script setup lang="ts">import type { LibraryItem } from './useAgentTeamDefinitionFormState'; defineProps<{ search: string; agentItems: LibraryItem[] }>(); defineEmits<{ add: [item: LibraryItem]; 'dragstart-item': [payload: { event: DragEvent; item: LibraryItem }]; 'update:search': [value: string] }>()</script>

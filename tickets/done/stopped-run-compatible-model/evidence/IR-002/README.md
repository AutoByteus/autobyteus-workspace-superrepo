# IR-002 — CRF-001 local-fix evidence

Completed 2026-09-09; package `stopped-run-compatible-model`; production/test commit `88afb0512964b59d4734117c01ee0cb6925c2e81`. Trigger: CRR-001 / CRF-001, SCN-005 / BEH-002 / REQ-006 / AC-007, AD-D02 / DS-06/07. Approved RER-004, AD-REV-001, latest ARCH-REV-002 and Medium/High classification unchanged.

## Bounded correction

The existing Team Save owner catches only the post-write canonical read failure. It retains `PERSISTENCE_INDETERMINATE` for a post-rename finalization uncertainty and returns it for an otherwise committed write whose canonical read throws or returns no tree. The last known tree is returned without claiming verification. A known `not_renamed` result remains `PERSISTENCE_FAILED`; a readable committed canonical tree still follows the existing exact comparison. There is no new lock, writer retry, rollback write, migration, revision token, runtime change or history operation.

The existing Web Team reconciliation path is reused. Successful canonical verification now clears stale error feedback observed during rendered inspection; no new UI state or workflow is introduced.

## Local checks and commands

All commands run from `/home/autobyteus/workspace/.codex/worktrees/stopped-run-compatible-model`. The retained logs span 2026-09-08/09; no API/E2E sign-off is claimed.

- `pnpm -C autobyteus-server-ts exec tsc -p tsconfig.build.json`: exit 0; refreshed production output before the physical-writer probe. `server-build.log` is intentionally empty.
- `pnpm -C autobyteus-server-ts exec vitest run tests/unit/agent-team-execution/team-run-model-selection-save.test.ts tests/unit/agent-team-execution/agent-team-run-manager-lifecycle.test.ts tests/unit/run-history/services/studio-run-model-config-service.test.ts tests/unit/agent-team-execution/team-run-model-config-mutator.test.ts`: **4 files / 23 tests pass**, `server-focused.log`.
- The expanded durable Team test exercises all nine combinations of physical result (`committed`, `renamed_finalization_indeterminate`, `not_renamed`) and canonical read (`ok`, exception, missing), through actual manager → Studio → resolver method/projection. Asserts expected outcome, last known/canonical pair, one write, retained tree/member/provider identity and settled task. This is method-level transport coverage, not GraphQL networking.
- `pnpm -C autobyteus-web test:nuxt stores/__tests__/existingRunModelConfigStore.spec.ts components/workspace/config/__tests__/TeamRunConfigForm.spec.ts components/workspace/config/__tests__/TeamScopeConfigEditor.spec.ts --run`: **3 files / 22 tests pass**, `web-focused.log`. New store cases cover deferred automatic verification, blocked Save while pending, successful first verification, failed verification → Retry, full canonical pair/planner installation, clean draft, cleared error and no second mutation.
- `node tickets/in-progress/stopped-run-compatible-model/evidence/IR-002/team-readback-probe.mjs`: exit 0, **both regression cases assert Pass**. Freshly built manager/Studio/resolver method plus strict current-schema store and actual temporary-filesystem writer. Original reviewer source copied to this implementation-owned path, with pass assertions; reviewer source/results unchanged. Injects only post-write read error; second case additionally directory-finalization error. Both write new model exactly once and now return `PERSISTENCE_INDETERMINATE` with the last known tree. Temporary directories removed in finally. This does not activate a saved product run, submit inference or validate replacement metadata.
- `node tickets/in-progress/stopped-run-compatible-model/evidence/IR-002/team-verification-render-inspection.mjs`: **local rendered check Pass**. Real existing Nuxt Settings components/Chromium, deterministic GraphQL + health transport. Changed-model Team Save returns indeterminate; automatic canonical read deliberately fails; Retry is visible; model controls/Save are locked; Retry reads canonical new pair, clears error, restores stopped editability and leaves no dirty Save. Exactly one mutation and three reads (initial, failed verification, successful Retry). No page/console errors. Owned Nuxt/browser processes and temporary fixture page cleaned up.
- Final images directly inspected: `rendered/team-verification-retry.png`, `rendered/team-verification-narrow.png`, `rendered/team-verified-narrow.png`, at 1280x900 and 520x900. No horizontal overflow at 520px. `rendered/initial-stale-feedback.png` is the diagnostic pre-polish image; current verified image has no obsolete error.
- `git diff --check`; both probe syntax checks; manual changed-source nonempty sizes **474** and **454**, round deltas **6** and **1**, below 500/220 guardrails. `source-size-check.json` retains the measurements.

## Limits / downstream

Implementation-scoped evidence only. No independent re-review Pass is inferred: CRR-001 remains Fail until Code Reviewer re-reviews CRF-001. No broader API/E2E, real changed-model Codex continuation, configured-member inference, compacted-history acceptance, full frontend typecheck/build or desktop shell validation was run in this round. The existing baseline static-test failures and all mandatory downstream gates remain visible in the cumulative handoff. Positive runtime metadata evidence from IR-001 is retained, not rerun or promoted to live acceptance. Text-log trailing whitespace is normalized only in this implementation-owned evidence directory.

# API-REV-002 — user-requested open_tab classroom test

Date: 2026-09-10 UTC. This directory E is under the assigned stopped-run-compatible-model ticket T. Worktree W: `/home/autobyteus/workspace/.codex/worktrees/stopped-run-compatible-model`.

**Requested model-switch journey: Pass.** Actual imported Classroom Simulation Team, Codex `gpt-5.6-luna` -> normal Team Stop -> Settings `gpt-6-astra` -> explicit Save -> normal messages. Both original professor and student provider conversations continued on Astra, retaining earlier context. **Overall supplemental validation: Fail /84.3%**, because the first student-to-professor tool remained pending with no visible approval before any model change; API-F001 requires focused failure-origin review. A later explicitly requested, focused-student retry worked but does not erase the initial observation. No claim that the model-switch implementation caused it.

## Setup / versions / ownership

- Intake HEAD `5f7a9b47e228e25529f5bd0acf80ea3a2142d303`, reviewed production `88afb0512964b59d4734117c01ee0cb6925c2e81`, Medium/High/Reviewed unchanged. `intake.json` pins agent repo HEAD `5612aa73f26ae4af218bd98269ad16d285ecfd3a`.
- User-named original local package `/home/autobyteus/workspace/autobyteus-agents` linked through actual Settings > Agent Packages > Import Package, not recreated synthetic definitions. It exposed8shared/54team-local agents and14teams. Source folder left in place, no definition edits.
- Actual Classroom Simulation Team: professor coordinator + student; both original tool configs `run_bash`, `send_message_to`. Classroom file work goes to unique owned workspace; auto-approve remains false. Individual inspected classroom message approvals accepted via visible UI.
- `node T/evidence/api-e2e-classroom/environment.mjs` starts existing reviewed built-server through documented `test-support/live-e2e/test-runtime-bootstrap.mjs`, unique DB/data and `RUN_CODEX_E2E=1`, retains supported existing CLI HOME authentication without copies/imports. Starts documented Nuxt `pnpm dev --port <reserved>` with `BACKEND_NODE_BASE_URL`. No production source patch, fixture route, GraphQL mock, direct model metadata manipulation or extra model override on resume.
- Owned env key `api-classroom-1789027849290`; backend33039, Nuxt42291. Existing shared app8000/data and Delivery's Electron build untouched. Existing Chromium accessed with actual `open_tab`, new tab1; no shared browser restart/profile reset.
- Platform Linux arm64; Node22.23.2, pnpm10.28.2, Codex0.153.4, Chromium151.0.7922.173, Nuxt3.21.1. Browser viewport1510×801, UTC. Desktop shell not exercised.
- Startup timestamps span08:10–08:36 and actual browser workflow22:47–23:06 in recorded environment; do not interpret this as continuous inference/test execution. Exact per-event timestamps are authoritative.

## UI procedure actually executed

Browser tools: `open_tab`, `dom_snapshot`, `run_script` for actual DOM clicks/input/change events, `navigate_to`, `screenshot`, `close_tab`. No private store mutations or direct mutation APIs substituted for requested UI steps. Passive HTTP observer and CDP WebSocket observer inspect only this owned tab, never intercept responses.

1. `open_tab(url="http://127.0.0.1:42291/settings")` -> tab1. Click Agent Packages button; fill `#agent-package-source` with exact user path; click `[data-testid="agent-package-import-button"]`. Visible success and network ImportAgentPackage recorded in `import-result.json`, `network.json`,02-imported.png. 01-import-form.png is only initial form, not successful import.
2. Navigate `/agent-teams`. Locate Classroom Simulation Team card, click its Run. In actual launch form select `#team-scope-root-runtime-kind` value`codex_app_server`, New workspace with `environment.json.workdir`, model list option GPT-5.6-Luna. No member override, tool auto-approve false. Click Run Team once. `launch-form.json`,03-luna-launch.png, `team-created.json`, network CreateAgentTeamRun success.
3. First normal textarea message to professor:
   > Let us test one short classroom exercise. Remember this private marker only in our conversation: maple-orbit-7391. Do NOT write that marker in any file or send it to the student. Ask the student to calculate 7 × 8 and explain the answer in one sentence, using your normal file-backed professor/student handoff. Use only this classroom workspace for files. Read the returned answer, then summarize the result to me with FIRST_LESSON_DONE. End the exercise there; do not send additional feedback or follow-up to the student when no further student action is needed.
   Click normal `button[title="Send message"]`. Professor runs pwd, writes homework in own classroom folder, requests send_message_to /student. Inspect exact provider tool arguments and approve visible bounded request. Student reads homework and writes answer56. Professor reads it and returns FIRST_LESSON_DONE.
4. **API-F001 observation:** student issues send_message_to /professor, but no completed tool/result/communication entry. Select student via normal sidebar: pending tool, no Approve. Normal supported deep-link reopen of same Team/student still no Approve. Provider tool timestamp22:54:05.224 -> before-stop snapshot22:56:46.526796 =161.3seconds. The initial “>5min” note was run-age confusion and corrected in canonical artifacts; `pending-duration.json` preserves correction. No initial approval WebSocket trace was collected, so emission-vs-rendering cause is NOT established. `student-pending-before-reopen.json`,04-student-pending-before-switch.png, before-stop-state.json.
5. Click visible `button[aria-label="Terminate team"]` once. Network success; member Offline. Open `button[aria-label="Edit Config"]`. Runtime stays disabled Codex. Root picker offers GPT-6-Astra, actual model options272000 equal to Luna272000 for every affected scope. Select Astra, expand member overrides; both professor/student follow root. Click `[data-test="save-existing-model-config"]` once. Clean canonical success, still stopped. `stopped-model-options.json`,05-astra-draft.png,06-astra-saved.png, saved-dom.json, save-assertions.json.
6. Back-to-events button, select professor, send normal textarea:
   > We are continuing the same conversation. What exact private marker did I give you earlier, and what numerical answer did the student compute? Reply only in the format: marker | numerical answer | SECOND_LESSON_DONE. Do not use tools, read files, or send any teammate message for this continuity check.
   **Observed:** `maple-orbit-7391 | 56 | SECOND_LESSON_DONE`, actual completed Astra turn, same provider/local IDs. No tools in this turn.07-professor-astra-resumed.png and professor-resumed-state/dom.json.
7. Select student, normal message asks earlier multiplication and answer, no tools/teammate messages; observed `I was assigned 7 × 8 and computed 56. STUDENT_RESUMED`, actual completed Astra turn on same original student provider ID.08-student-astra-resumed.png, continuation-assertions.json.
8. Bounded failure-origin control C19: with student already selected, explicitly request existing answer-file handoff again (no file rewrite). Passive WebSocket sees TOOL_APPROVAL_REQUESTED; visible Approve appears. Inspect exact /professor/owned-file request; approve. WebSocket APPROVE_TOOL, TOOL_EXECUTION_SUCCEEDED and DELIVERED plus persistent communication entry, student completes. Professor later asks read-only get_handoff_rules, inspected/approved; final response acknowledges delayed handoff, no follow-up needed.09-focused-approval.png, focused-handoff-assertions.json, focused-handoff-websocket.json. **This explicit retry succeeds; original background attempt unresolved, not retroactive Pass.**
9. Stop entire owned Team again, confirm final stopped Astra Settings10-final-stopped-astra.png; preserve own run/files; close only tab1, signal only owned observers/environment, confirm cleanup. No shared process shut down.

## Independent assertion sources

`capture-state.py <phase>` reads only this Team's exact persisted tree and provider IDs, locates matching CLI session files, filters relevant tool/turn/completion records (no encrypted reasoning copied), hashes local non-tree files/workspace and exact provider transcripts. Run at before-stop, before-save, after-save, professor-resumed, student-resumed, after-focused-handoff, final-stopped. These sources are audit scaffolding tied to owned live state, not blindly rerunnable after cleanup. Adapt output/environment to fresh owned fixture to repeat; do not overwrite this evidence.

| Scope | Same local Agent / provider ID | Actual provider turn models / completed result |
| --- | --- | --- |
| Professor | `professor_4f3d07980e364d4982061dacaa5c0dc9` / `01a08d84-dd94-78d2-9eb5-dfec9449040d` | Luna original completed first lesson -> Astra22:59:24.974 turn_context and22:59:28.893 completed exact private marker+56 |
| Student | `student_68f0238b837845c2a7cb574b2178e557` / `01a08d86-e043-7391-b471-9670360878c1` | Luna original math/tool turn (return interrupted) -> Astra23:01:14.738 turn_context and23:01:18.025 completed earlier arithmetic; later Astra handoff retry delivered |

Root Team ID `classroom_simulation_team_e9eca93f43d34342851d79f7563ec58f` unchanged. Save assertions prove one mutation, exactly /,/professor,/student patches, fixed tree/runtime/policy/topology/task arrays unchanged, both provider IDs+entire transcript hashes unchanged during Save, all local history/workspace bytes unchanged. Private marker absent from classroom files and from the second question. Attribution is independently read provider turn_context + matching completed turn, not model self-identification or catalog alone. No claim to inspect hidden provider weights.

`retained-run/` preserves only this stopped Team's current tree/local traces/messages/task records; `classroom-files/` contains original two exercise files. Original provider transcripts remain in CLI history under exact paths in assertion JSON. No unrelated data copied.

## Result / limits / cleanup

Cases C15Pass, C16Fail(initial return tool pending; normal Stop itself Pass), C17Pass(core model switch), C18Pass(student continuation), C19Pass(focused handoff reattempt). Initial symptom’s root cause is Unclear; not assigned to model-selection code just because discovered during its validation. Current new API-F001 is the reason full supplemental round is not clean Pass; reviewer must determine implementation/test/environment/preexisting origin. Source selection behavior is directly positive.

No production or durable test changes this round. Three prior reviewed durable test hashes match exactly (`durable-reuse-audit.json`); API-REV-001248tests/6browser cases and build remain attributed previous results, not rerun. No live new compaction, Claude/native matrix, narrow viewport, Electron/desktop packaging/full Web build/typecheck rerun here. Delivery's separate work is not claimed.

Helper corrections: async-arrow run_script returned null without import execution; used supported synchronous DOM scripts and accurately named initial form screenshot. C19 summarizer initially counted another professor rules approval; scoped assertion to exact student invocation and corrected an append import typo. These did not change application behavior. Initial pending duration corrected from sidebar run age to actual161.3s. Server log includes CODEX_SEGMENT_TURN_INACTIVE after stopping the interrupted student; no unproven root-cause inference from it.

`cleanup-audit.json`: owned runtime/DB/server/Nuxt/observers removed/exited; shared browser39/server42 still running. WebSocket observer persisted frames, then detach raised because owned tab had already closed (exit1); audited process is gone and data retained. Environment/HTTP observer exit0. Browser not globally closed. Original agent repo and shared auth/config untouched; exactly2 new provider sessions and filtered evidence intentionally retained. Heuristic evidence-only scan found no common secret candidates, not a universal guarantee. No commit/staging/release/final delivery performed.

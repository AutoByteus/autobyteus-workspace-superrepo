# Flat AgentTeam Domain Simplification — Design Spec

## Status

`Not started — Draft requirements approval and deeper investigation required.`

This placeholder is intentionally present because `design-spec.md` is a mandatory solution artifact. It is not implementation authorization and must not be treated as an approved design.

Before completing this artifact, the receiving solution designer must:

1. obtain approval of `requirements.md` and `flat-agent-team-concept.md`;
2. refresh/reconcile the ticket against the then-current `origin/personal` without modifying the separate dynamic-AgentTeam worktree;
3. trace configured Team definition, launch, runtime, persistence/history, GraphQL, frontend, and task-delegation production spines;
4. inspect representative persisted nested definitions and TeamRun packages;
5. establish the precise native AgentOrg prerequisite and rollout order;
6. separate configured nested Team removal from supported task-scoped Team execution;
7. produce the complete design using the authoritative solution-designer template and design principles;
8. append a new `SR-*` entry before architecture-review handoff.

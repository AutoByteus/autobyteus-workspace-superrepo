# IR-045 local implementation checks — CR-FIND-035

## Scope and reproduction
Only the shared textarea production source is changed. The current transport/submission/hydration owners and API schemas are unchanged. The two durable actual-textarea regressions first ran against the original IR044 production code and failed exactly as reviewed: a typed-then-cleared next draft was restored as the prior submitted text; a held verified same-Agent replacement published an empty draft. See before-fix.log (expected exit1, two failed cases /12 skipped), not a current-package pass. No fake clocks, source instrumentation, schema relaxation or lifecycle commands were used.

## Commands / current results
All commands use PATH=/tmp/aorg-ir035-bin:$PATH. Root is the ticket worktree.
- Existing prerequisite builds: `pnpm -C autobyteus-application-sdk-contracts build`; frontend SDK build; backend SDK build. All three dist directories were absent at entry and are removed after validation; existing other outputs are preserved.
- Focused `pnpm -C autobyteus-web test:nuxt --run services/agentOrgExecution/__tests__/agentOrgComposerSubmission.spec.ts components/agentInput/__tests__/AgentUserInputTextArea.spec.ts`: **2 files /30 tests pass**.
- Cumulative affected concrete paths retained from CRR068: `pnpm -C autobyteus-web test:nuxt --run <79 paths from web-paths.txt>`. The list was checked for exactly79 existing files before invocation; no empty/directory filter can start an unintended broad run. **79 files /669 tests pass**, including a final rerun after the extra disabled-Send assertion. Counts overlap, not additive.
- Both frontend boundary guards and mandatory localization literal audit, then production Nuxt build: **pass /0 unresolved literals and16 prerendered routes**, recorded in guards.log/build.log. No full frontend typecheck, server/provider or native Electron package claim.

## Executable boundaries
Actual shared textarea -> existing exact-context setter -> real Pinia active/Org stores -> existing submission/restoration and strict hydration/publication. All input edits are committed synchronously to the existing AgentContext; no debounce queue/flush, revision token, extra cache or owner. Typing then clearing now produces actual successive state changes seen by the existing submission guard. Snapshot publication sees the latest stored unsent draft before it copies it.
The replacement regression holds external member projection completion, types through the actual visible textarea, then releases verified replacement and checks the new exact context and visible text, attachments, canonical empty projection, live phase and zero send/replay/history effects. The rejection regression types the original submission and attached file, types and clears a newer draft, then rejects the exact ACK; it checks empty current/visible draft and attachments, pendingfalse, Send disabled, one original send and no history navigation/refresh. Existing untouched rejection restoration, focus switch, normal hello/one repeated echo, canonical status, null navigation and Team behavior are retained. Prior setup tests that directly assigned context remain narrower evidence, not substitutes for these paths.

## Rendered feedback
Normal isolated Nuxt dev43145/native Chromium, actual shared Agent/Team surfaces/composer/stores/service and intercepted strict snapshot/member projections/ACKs. Ten states at1440x900 and390x844, with touch and keyboard. In addition to prior normal-send/newer-draft/error states, actual browser checks cover:
1. A submitted file and text, typed-then-cleared next draft, rejected ACK: composer stays empty, attachments stay empty, Send is disabled, no second send.
2. Existing automatic recovery with projection completion deliberately held, typing while the same Agent remains visible, then release: current and visible draft remain exact after verified replacement, canonical conversation comes from the new projection, no send/replay.
Ten screenshots and evidence.json inspected directly; no pageerrors/horizontal overflow. The diagnostic header and intercepted I/O are fixture-only, not new product UI or a provider/native/user-data journey. The browser gate waits for actual projection ingress; it does not assume cold-click timing. No production timers/retries or polling were added. Temporary page/process are removed; the live DR009 app is not touched.

## Concurrent scope / limits
Architecture independently committed AD023@5403b798194bce3e5b7bdf9c0fe36d91f6cd02b7 while this Local Fix was validated. ARCH020@bf3089ee82af7fe75c3856a0bd45652ffbb08146 subsequently passed that design after this correction source commit. The new history-continuation-stop implementation is not claimed by IR045. Coordination was sent to the existing Architecture Designer before coding; the shared input-only correction does not relocate submission or introduce a competing owner. Current approved basis for this correction remains RER032/AD022/parent ARCH019; the newly approved AD023 requires a separate implementation reconciliation, not acceptance inferred from these composer checks. API27/DR009 and all older recorded observation limits remain scoped; source review, renewed API/E2E and current Delivery build/user verification remain required.

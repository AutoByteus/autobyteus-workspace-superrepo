# IR-050 — Implementation-scoped checks

## Outcome and scope
Ready for cumulative source review on source `3005c8921781108af7d4bef9938c26c7f6aaf53a`, not API/E2E or Delivery approval. This completes the combined IR048 authoring/frontend work and reviewed AD025 exact-file ownership/initial-cutover code. The user withdrew the blanket IR049 coding hold; implementation and disposable-data tests resumed. No real installation cutover was performed or authorized.

## Executed checks
- **Frontend: 87 files / 717 tests Pass**, concrete `web-paths.txt`, `web-cohort.log`. Includes actual shared input/Pinia/Org submission/upload-store integration with bounded external I/O, direct/mounted ownership, inactive preparation, remove/clear/open, pending send and attachments, focus/root switches, newer draft/discard, canonical echo, Team publication, history/retained navigation/localization regressions.
- **Server: 35 files / 203 tests Pass**, concrete `server-paths.txt`, `server-cohort-final-current.log`. Includes three actual Fastify multipart/REST/stored-location/filesystem tests, eight migration locator tests, two shared physical writer tests, complete production registry/current SQLite repository/runner on a fresh disposable pre-ticket fixture, authoring/API/package roundtrips and cumulative runtime/task/summary/strict readiness tests. Repeated focused runs overlap these totals; do not add them.
- Exact Org REST tests use configured/task Agents with repeated direct/mounted addresses and distinct file bytes. Actual upload, draft GET/DELETE, exact finalization, final GET/reopen and provider-local-path equality pass. Wrong/missing/extra/retired/unsafe owners and mismatched pairs reject; partial-batch filesystem rename failure preserves completed bytes and later explicit finalization completes existing semantics.
- Migration tests prove source-slot physical validation before rename; containing-Team/address versus root/address mapping; unique physical owner including retained task; current exact local lookup after rename; raw active and archived traces, formal message/task/update reference fields and cross-view/cross-root references; unchanged unrelated values, nonchanged JSONL lines/terminators and binary bytes; flat tree bytes; current zero-write; missing/ambiguous ownership, pre-rename/indeterminate/after-commit/reread failure and ordinary interruption retry; target-only cleanup and readiness exclusion/read-only recovery. The current archive fixture includes its manifest and is read through the real current RunMemoryFileStore after migration.
- The complete production registry test now includes both pre-ticket authored definitions and a representative released one-level Team runtime containing a saved attachment, plus a native flat Team. It verifies final unversioned/org_local output, exact final attachment URI/bytes, flat runtime tree equality, external file equality and completed-entry no-replay. This is a fresh disposable fixture, not an execution against any inventoried installation; it is not a universal old-format/deployment claim.
- Server `build:full` Pass, including TypeScript and sanitized built-module/bootstrap smoke without DATABASE_URL (`server-build-final-current.log`). An early no-emit attempt lacked the initially absent SDK dist prerequisites; after their normal builds it passed. No source relaxation for setup errors.
- Production web build/prerender **16 routes Pass** (`web-build.log`), after removing the temporary renderer route. Web/localization guards and mandatory literal audit **zero unresolved findings** (`guards.log`). Existing Browserslist/KaTeX/SQLite/MODULE_TYPELESS warnings remain disclosed, not suppressed.

## Rendered implementation validation
`render/evidence.json` and `render/result-3.log`: **18 observations / 9 screenshots**, desktop 1440x900 and narrow 390x844. Directly inspected narrow uploaded inactive and desktop sent states: established shared input/file presentation, no overflow, correct Offline/Idle and cleared composer, file labels/removal affordances and readable retained content.

Actual browser file chooser and click/open use actual HTTP Fastify routes/services backed by a newly created disposable stored Org tree/filesystem. The harness forwards requests/responses unchanged to its isolated server, with only test CORS adaptation. It does not fabricate upload/finalization/read results. Text bytes, PNG rendering, final/retained URLs and exact AgentRun ownership are asserted. A held actual upload response resolves into the captured direct-Agent context after selecting the mounted Agent and composing a newer draft; the new context/draft is unaffected. Preparing/opening while inactive performs no Restore/Send. Remove/clear do not delete retained final files.

**Limits:** Header subject/stop controls are diagnostic fixture controls, not a full production navigation journey. GraphQL inspection/restore, stream/provider acceptance and conversation events are synthetic. Four Sends/five explicit Restore-or-Stop mutations exercise existing frontend submission/continuation, not a real provider. Retained media proof is PNG; no general claim for arbitrary provider-owned retained text-file history. Task exact REST reads are verified in the backend tests, not task-center browser behavior. Standalone Team regression is in the durable affected cohort; no new full standalone real-provider journey. No native-shell or user verification.

## Iteration disclosures
- Initial migration fixture omitted its task record while retaining a settled task node; strengthened full-package validation required a coherent accepted task record. The readiness test also needed the same configured origin as its disposable migration. Corrected fixtures, no weakening of runtime validators.
- Initial flat-index calls assumed methods from the Org index; corrected to the existing Team direct-agent/direct-Team traversal and physical-scope method. Duplicate failure counting was corrected by retaining one root inventory failure rather than scanning its already-invalid sidecars again.
- An existing location test still supplied the removed address-only Org owner; it now uses the exact AgentRun contract while retaining cross-root isolation assertions.
- The raw archive fixture/scanner was corrected to the actual current root-level segment layout; original archive filenames/manifest and record IDs remain unchanged.
- First browser runner observed the visible upload placeholder before actual HTTP completion and asserted an absent request result. Corrected the runner to await the actual upload response; no product workaround. The final 18-observation run passed.
- A shared-writer self-check caught that moving JSON serialization outside its old try block could alter original error-result semantics; the original non-throwing pre-rename outcome is preserved and durably tested.
- Initial staging whitespace check found a trailing blank line in a new projection file; removed before source commit. Failed and intermediate local logs are retained; final source/checks are authoritative. Raw runner logs are preserved verbatim, including trailing spaces/blank lines reported by Git; source and authored-document whitespace checks pass.

## Preservation / remaining work
All **17,090** starting other-owner hashes matched again at completion (`completion-integrity.json`; entry comparison in `preservation.json`). Protected API model probe, source-bound documentation, reviewer/API/Delivery records and raw DB/env/key evidence remain untouched. Only 27 explicit source/test paths were committed in this round; IR048 contributes the retained authoring changes (39 combined paths). Source maximum is 497 effective nonempty lines. The new migration planning file's >220-line delta was assessed and split by concern: tree projection, typed record traversal and current readiness remain separate; one ephemeral migration plan owns only old-selector proof and commit orchestration.

IR049's actual-root inventory is retained as **installation-specific cutover evidence**. The absent historical vncuser path never blocks code. The sampled `/home/autobyteus/data` terminal family record plus old locators must not be reset/replayed or claimed migrated. Any actual rollout to that condition still requires its separate transition decision. No live root was mutated, no new migration ID/registry entry, no normal old-route alias, no runtime/schema/task/queue/lifecycle change.

Fresh whole-ticket source review, then the full renewed API29 held matrix and applicable proportional API-test review remain required. Upload AND click/open must be executed through the actual file chooser on direct/mounted inactive and active targets, including sent/retained references, no-activation preparation/read, focus/newer draft, partial failure and standalone controls. Preserve all prior API27/CRR059/CRR067/IR041/DR007/DR009 evidence limitations; unknown historical stalls/API20 first guard/delay remain unassigned. Delivery owns a fresh final build, user verification and any cutover/release decision.

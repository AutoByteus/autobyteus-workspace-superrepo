# IR058 local implementation validation

Authority: RER033 / AD026 / ARCH023 Pass / AAV003. Trigger CRR087 / CR-FIND045 / API36 / API-FIND038 / DS037 / VAL058. REQ031 / AC026 independently authorize truthful stopped root controls while refresh is pending.

## Spine and correction
Normal collection Stop -> typed subject action -> Org context store -> existing terminate adapter -> confirmed success -> markHistorical -> applyAgentOrgActivity -> existing navigation topology publication -> cached workspace/Org group -> actual root badge/control. History slice and generation already changed synchronously; the projection retained the previous row object until refresh returned. Add the missing existing publication call before starting refresh. No new owner/cache/flag/generation, fabricated Agent status, summary mutation, persistence, shutdown or Restore changes.

## Commands / evidence
All pnpm commands use PATH=/tmp/aorg-ir035-bin:$PATH from the assigned worktree; test paths are relative to autobyteus-web.
- Three application SDK prerequisite builds Pass (`prerequisites.log`); they were absent on entry and removed after checks. Preexisting core/server dist retained.
- Initial `test:nuxt stores/__tests__/runHistoryStore.spec.ts components/workspace/history/__tests__/WorkspaceAgentOrgActivityPublication.spec.ts --run`: 6Fail/41Pass. Two store assertions reproduced the omission; four new component tests failed from a test-only nonexistent activeTarget accessor. Corrected to selectedTarget(); not a product failure.
- Prepared before-fix: 4Fail/43Pass across two files, precisely two real cached projection and two actual root-row pending assertions. Both failed-Stop controls Pass. No production correction yet (`before-fix-prepared.log`).
- Corrected focused: 2files47Pass; expanded final focused: 2files50Pass. Both release/error orders, true/false confirmed activity, unrelated-row preservation, direct/mounted exact selected contexts/conversations, successful/unavailable stopped inspection, pending termination non-optimism and rejected Stop controls. One exact terminate, no Socket Send, no Restore. Tests control transport, not production state owners.
- Frontend cohort commands: `pnpm -C autobyteus-web test:nuxt $(cat /tmp/aorg-ir058/web-paths.txt) --run`. Initial102files840Pass; expanded102files842Pass; final selector-aligned completion102files842Pass in two disjoint51-file commands (web-paths-a/b). The first completion attempt ended SIGTERM/143 before a summary with no failed assertion: cause unknown, not counted as Pass; original log and disposition retained. Partitioned rerun changed no source/assertion. Overlapping counts are NOT additive. Existing composer/readiness/retained-status/authoring/navigation/attachment coverage retained.
- `pnpm -C autobyteus-web build`:16 prerendered routes Pass after diagnostic route removal. `guard:web-boundary`, `guard:localization-boundary`, `audit:localization-literals`: Pass. Existing build/dependency warnings retained. No server/core/native/Electron/full vue-tsc/all-tests-TS validation claim.

## Actual rendered loop
Own Nuxt dev renderer on127.0.0.1:43158 plus own Chromium, diagnostic route/controlled Apollo and Socket/synthetic taskBearingView. Real history/navigation projection, actual collection/root row, typed Stop action/context/run owners, full selected AgentOrg workspace/monitor/composer. Not normal Workspaces/server/provider/API execution or native shell. Non-own-origin network and GraphQL/REST blocked; icon-network glyphs unavailable, so no icon-fidelity/redesign claim. Diagnostic responsive composition/forced expansion, not full app-shell geometry.

Eight final cycles: widths1502x900 and390x844 × direct /director and mounted /team/lead × successful/rejected Stop.32 observations and20 final screenshots. Keyboard Stop, Running/enabled active; Running/disabled while termination pending; successful Stop gives grey Stopped/Stop absent while old+follow-up history remain held, same exact selected context/selection/conversation/draft and history route/Offline; releasing old active response and rejecting follow-up retain the inactive row. Rejected Stop retains Running/enabled control, visible error, live context, exact selection/conversation and active route. Zero Send/Restore/pageerror/overflow. Existing real tree selection predicate distinguishes configured and task executions sharing an address. Capture waits350ms for established color transition only; history remains held throughout.

Temporary harness dispositions: initial render timed out because fixture mounted workspace before controlled live snapshot finished, causing ordinary open to replace that controlled stream; fixture now waits for live before mount. Next attempt sampled route before asynchronous router completion; inspector now awaits exact history mode. A pre-final32-observation pass used an address-only diagnostic selection predicate and captured header color transitions mid-animation; final diagnostic uses existing tree-state identity predicate and settles transitions before screenshots. Logs/evidence retained; these are not product fixes or missing API evidence reconstruction. Only source correction is the two-line history publication delta.

Current pending and settled screenshots directly inspected for root grey/green, Stop absence/presence via DOM, exact selection, readable summary/conversation/draft, spacing/overflow and narrow wrapping. All20 final screenshots inspected. Failed Stop preserves the existing generic workspace notice plus root-specific Stop rejected error; that notice selector is unchanged and not newly adjudicated by this publication fix. API36 original actual hosted proof remains historical; its missing narrow pending and obsolete-response-after-consumption claims are not filled by these controlled local checks.

## Boundaries
No API acceptance, confidence score, successful proportional test review, Delivery/nativeShell/AppImage/user/release readiness. API36 Fail78.6 and all held scope retained in handoff. Three API-owned unstaged tests unchanged. IR049 actual-installation cutover decision remains Architecture-owned before cutover, not a coding hold. DR009 and live data/auth/migrations untouched.

Artifact packaging note: whole-artifact staged whitespace check returned nonzero on verbatim test/build logs and raw diff context lines. Source and canonical handoff/revision checks passed. Raw evidence is retained byte-for-byte rather than cosmetically rewritten; this check is not a source/build failure. An early final verifier consequently stopped at the nonempty staging assertion before artifact commit; rerun after the explicit artifact commit is authoritative.

# IR-044 — Local implementation checks

## Scope and commands
Bounded COMPOSER-001 correction; implementation validation, not API/E2E sign-off.
Working directory: autobyteus-web unless stated otherwise. All pnpm commands use the existing PATH=/tmp/aorg-ir035-bin:$PATH Corepack shim.

- Final cohort: `pnpm exec vitest run services/agentOrgExecution/__tests__ services/runSubmission/__tests__ services/agentStreaming/handlers/__tests__/memberInputMessageHandler.spec.ts components/agentInput/__tests__/AgentUserInputTextArea.spec.ts stores/__tests__/agentTeamRunStore.spec.ts stores/__tests__/activeContextStore.spec.ts stores/__tests__/agentOrgContextsStore.spec.ts`: **13 files /106 tests passed**. See cohort.log. Earlier overlapping 4/47, 13/105, 2/29 runs are not added.
- `pnpm guard:web-boundary`, `pnpm guard:localization-boundary`, `pnpm audit:localization-literals`: passed, zero unresolved findings.
- Existing prerequisite `pnpm -C ../autobyteus-application-sdk-contracts build`, followed by `pnpm build`: **passed, 16 routes prerendered**, recorded in build.log; no full typecheck or Electron packaging claim. The SDK dist was absent at entry and the generated prerequisite is removed at completion.

## Durable production-shaped proof
New agentOrgComposerSubmission.spec.ts runs the actual shared textarea, active-context dispatch, Org store/service, strict snapshot hydration, exact AgentContexts and shared event reducer. Only external Apollo/WebSocket I/O and unrelated voice readiness are doubles. Direct configured, mounted configured and task-Team Agents each clear exact text/attachments immediately, remain pending until canonical status, and merge local plus repeated authoritative echo into one message. Only accepted ACK requests authoritative history; no Team navigation effect or optimistic Org title.
Failure controls preserve untouched retryable text/attachments, newer exact-context text/attachments, deliberate clearing and attachment-only drafts, focus-switched other drafts, and newer textarea input inside the 750ms debounce interval. A synchronous transport failure retires its command/timer without replay. Disconnected preflight consumes nothing; in-flight release affects only the captured context. A complete checkpoint-verified replacement preserves each exact AgentRun's unsent draft while its conversation/status come from the new strict snapshot/projection; no command replay or new recovery path.
Existing Team submission, local navigation, shared input, status/echo, strict Org generation/ACK/checkpoint/five-attempt recovery, task reactivity/parity, hydration and focus tests pass.

## Corrections during validation
The first new echo fixture incorrectly used input_origin=user and an extra recipient_agent_run_id inside the root-neutral presentation payload. Strict decoding correctly rejected it; three new tests failed after failure restoration. The fixture was corrected to the actual contract (user_message, exact recipient in the surrounding event), without changing schema admission. Final assertions check accepted timestamp and live phase before ACK, so a local-only message cannot masquerade as a received echo. Initial log remains /tmp/aorg-ir044-focused.log; corrected/final evidence is retained here.
The newer-draft test also explicitly covers a pending textarea debounce plus submitted attachment: synchronous flushing on pending completion makes the exact owner see the new draft before considering restoration. No second draft store or composer is introduced.

## Render loop
Normal isolated Nuxt dev on127.0.0.1:43144; native headless Chromium. Actual AgentWorkspaceSurface/TeamWorkspaceSurface, AgentEventMonitor, composer, Pinia/context/service and native socket with intercepted synthetic current snapshot, member projection and exact command replies. Six states at1440x900/390x844: direct pending; successful direct reply with empty composer; mounted pending; mounted reply preserving a newer draft through focus switch; rejection preserving newer unsaved text; untouched rejected draft restored. Touch, keyboard Enter, zero pageerrors and no horizontal overflow. Direct visual inspection confirms clear/disabled empty Send, pending state, single user bubble and assistant reply, readable existing error feedback, usable narrow composer. Diagnostic selector/header and synthetic I/O are fixture scaffolding, not UI changes or the live user-data/provider route. Attachment and checkpoint-recovery proof is unit/integration, not browser file-upload/recovery proof.
See render/evidence.json, fixture.vue, inspect.mjs and six screenshots. Temporary page/process are removed; the live DR-009 Electron app is untouched by Implementation. Later API/E2E must assert actual composer text, attachments, pending and one echo—not only ACK/reply—on the renewed package. Delivery owns fresh package/user verification.

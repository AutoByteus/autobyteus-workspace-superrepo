# IR-047 — coherent Team execution publication

## Authority and scope
RER-032 / AD-REV-023 / ARCH-REV-020 Pass, cumulative Large/High. CRR-070 / CR-FIND036 / API-REV-028 / API-FIND030 requested this implementation Local Fix. Source is 37200cbe75a927ed2ad02c829d08e30b1c4d9239, based on 63aae709e2fd9f5ab190e82885c9f14d07b98d07. The latter adds Architecture investigation only over IR-046 artifact b3de58a24bcfc0173ae414924b5079e1f7ead838; the tested prior production source is 61f633abe669d1283a617847c7294ce7b8047bb6.

One production file changes: teamExecutionViewState. Its existing owner privately prepares new reactive AgentContext associations in a plain candidate Map, then publishes the complete validated tree, locations, contexts, task records, messages and sequence through one shallow ref. Matching AgentContext instances stay reactive and retain identity. Snapshot status projection and existing effects/focus repair run with complete associations available. All consumers use that publication; there is no missing-location skip, relaxed admission, second cache or delayed consumer workaround. The shared composer's immediate actual-edit semantics are unchanged.

Initial construction occurs before exposing the view. Activation, settlement and reconnect snapshot publication no longer expose association-first reactive Map writes. Root activity, retained/live focus rules, strict sequence/root/placement checks and effect dispatch retain their current owners and behavior. The source change is +71/-62 (133 changed lines), 420 nonempty lines, below both guardrail signals. No public contract, server/runtime/lifecycle/API/schema/migration/Product change.

## Durable regression and negative control
`components/agentInput/__tests__/TeamComposerPublication.spec.ts` mounts the actual TeamWorkspaceSurface, AgentEventMonitor, shared textarea, real Pinia active/selection/Team-context stores, real Team view and TeamStreamingService. Only external history I/O, voice and the WebSocket transport are doubled. Valid fixtures are current strict flat Teams: configured /lead and /verifier, then exact fresh task AgentRuns at the same /verifier address. Every admitted wire fixture passes the production parser before dispatch.

- Selected composer stays mounted while activation1 and status2 apply; exact task row is Running, task history is present, sequence reaches2 and no warning/recovery/disconnect occurs. A synchronous witness compares every exposed context ID with tree placement and task IDs at each publication, not just after return.
- Exact task input3 reaches the real event monitor; configured same-address context remains empty. Returning to lead retains its actual textarea draft and same AgentContext object. Deliberate clear persists; no sends occur.
- Reconnect snapshot7 introduces two repeated-address tasks while the composer stays mounted; status8 reaches the exact second task. Every synchronous witness is coherent, draft/focus/AgentContext are retained, no warning or send occurs.
- Invalid snapshot missing canonical statuses and activation targeting a different parent are rejected without any published association/tree/sequence/task change, draft consumption or focus loss.

Before the production edit, the two positive cases both fail on the intended missing-location boundary through activeContextStore and the synchronous shared composer, then show stalled sequence0. `before-fix-strict.log` records this. The first fixture attempt also supplied snapshot-only member_address to strict AGENT_STATUS event payloads; this was rejected by the unchanged parser. That setup mistake is disclosed in `initial-fixture-extra-status-field.log`, and corrected before the decisive negative control. No production schema relaxation or timeout change.

## Local checks
All commands run from the assigned worktree with PATH=/tmp/aorg-ir035-bin:$PATH.
- `pnpm -C autobyteus-web exec vitest run <concrete web-paths.txt>`: **80 files / 689 tests Pass**, cohort.log. Prior79-path affected cohort plus the new4-test integration suite; no directory selectors. This includes retained Org input/inspection/continuation/Stop, Team submission/focus/settlement, strict stream and task/Messages/localization regressions. Counts overlap with all focused runs.
- Earlier focused Team view / stream / new composer checks: **3 files / 25 tests Pass**, focused.log (before adding two invalid-publication cases). Final4 new tests are included in the80-file cohort.
- `guard:web-boundary`, `guard:localization-boundary`: Pass. Mandatory `audit:localization-literals`: **0 unresolved**, guards.log.
- `pnpm -C autobyteus-web build`: Pass, **16 prerendered routes**, build.log. Own diagnostic route was removed first. Known bundler/chunk/dependency-age warnings remain nonblocking; no production build configuration changed.
- Full typecheck unavailable: `pnpm -C autobyteus-web exec vue-tsc --noEmit` reports missing executable (exit254). Build is not a full typecheck.
- Three initially absent application SDK contracts/frontend/backend dist prerequisites were built for checks, then removed individually after verifying they were not tracked or symlinks. Existing build outputs preserved. No server/provider/Electron packaging run in this frontend-only local round.

## Rendered feedback loop
Normal Nuxt development renderer on isolated43147, native headless Chromium, actual TeamWorkspaceView -> TeamWorkspaceSurface -> monitor/composer, real active Pinia and strict stream/view publication. Synthetic exact flat Team and injected transport; external history refresh/projection reads replaced with no-op I/O. Diagnostic header buttons select exact IDs, not the production Workspaces tree. This is implementation rendering, not independent API/E2E.

Eight states at1440x900 and390x844: before activation with a real typed draft; activation/status with the same draft; exact task monitor input through keyboard (desktop) or touch (narrow); reconnect snapshot with two same-address tasks while lead remains selected. Sequence0->2->3->7->8, all synchronous association witnesses coherent, same lead AgentContext, exact Running task rows, retained/cleared textarea and zero sends. No page errors, matched Team rejection warnings or horizontal overflow. Screenshots directly inspected for unchanged header/status/task text, monitor content, focus, usable narrow controls and composer layout. No production styling or copy edit needed.

`render/evidence.json` is the recorded result; fixture.vue/inspect.mjs are safe reproducible diagnostic sources, not shipped routes. The current actual provider incident and full history/navigation/locale/native-shell matrix remain downstream work. The rendered task status is synthetic strict input, not a claim of real provider progress.

## Preservation and remaining gates
**14862 starting other-owner path hashes,14862 unchanged**, preservation.json. No other-owner source-bound docs, Code Review/API/Delivery reports, tests, raw evidence, DB/env/key files were edited/staged/reset/claimed. The source commit contains exactly one production and one test file. Own temporary route/process and initially absent SDK outputs removed; existing DR-009 Electron listener31009 untouched. Only safe synthetic logs/screenshots/inventories are included in this evidence directory. Log copies normalize trailing whitespace only.

Fresh cumulative source review is required before full renewed current-artifact API/E2E. API-REV-028 remains Fail74.0 with its matrix incomplete; repository324paths/1988tests already includes Electron9/39, not additive. Its real browser failure and all held task/Org/operation/model/history/locale/migration/recovery/shutdown groups, including VAL-054–058, must be reconciled by API/E2E. CR-FIND035 remains source-resolved; this fix does not reverse immediate draft tracking. Prior API27/CRR059/IR041/DR007/DR009 and unknown historical stalls/API20 first-guard/delay origins remain scoped. No Delivery/package/native/user/release readiness is claimed.

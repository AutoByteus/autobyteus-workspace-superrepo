# Requirements Document

## Document Status

- Status: `Approved Architecture-Ready — Mounted Team Status Product Supplement Approved`
- Current requirements revision ID: `RER-021`
- Request / ticket: `AORG-FLAT-TEAM-001`
- Requirements owner: Requirements Engineer
- Date: 2026-09-01
- Approval state and reference: The cumulative behavior, runtime, definition-admission policy, approved `RV-012` experience, and `RER-019` default-baseline activation remain authoritative. `RER-020` records the user-approved mounted-Team aggregate status correction. Product Prototyper returned the focused `AORG-FLAT-TEAM-STATUS-001` supplement with explicit user approval on 2026-09-01, normative `VIS-STATUS-001`–`VIS-STATUS-003`, and no requirement-impact finding. `RER-021` integrates that Product authority without changing the approved semantics or reopening unrelated `RV-012` behavior and returns the cumulative package to Architecture Designer for the `API-FIND-007` impact revision.

## Problem And Desired Outcome

- Problem: `AgentTeam` currently represents both an ordinary working Team and an arbitrarily recursive organization container. The overlap makes authoring, execution, persistence, handoffs, APIs, and UI harder to understand and maintain.
- Affected actors or systems: Package authors, users launching or inspecting collaborative work, Team-bound Agents, definition and execution services, persistence/history/memory, collaboration and task contracts, GraphQL/stream contracts, and the web client.
- Desired outcome: Introduce one distinct `AgentOrg` composition root containing independent Agents and flat, coordinator-led Teams. Each Team contains Agents only. Organization-scoped handoffs connect peer Teams and independent Agents without persistent Team-within-Team nesting.
- Observable success: The Software Development Department and Northstar structures remain representable with the same effective members, Team ingress, canonical destinations, and workflows, while new Team definitions cannot contain Teams.

The governing principle is: **containment expresses stable membership; handoff rules express workflow.** “Flat” does not mean unrestricted all-to-all communication.

## Relevant Current And Desired Behavior

| Behavior ID | Kind | Evidence-Backed Current Behavior | Desired Behavior | Intentionally Preserved Behavior | Investigation Evidence |
| --- | --- | --- | --- | --- | --- |
| BEH-001 | Contract | One AgentTeam definition may contain Agents or AgentTeams recursively. | `AgentOrg` is the only persistent multi-Team composition root; a Team contains Agents only. | Team names, instructions, ownership scopes, Agent refs, and direct Agent coordinators remain available. | Domain model, graph resolver/validator, package inventory. |
| BEH-002 | System | A launch resolves a recursive Team graph into one root TeamRun and child TeamRuns. | One AgentOrg launch owns one collaboration/address/lifecycle scope containing direct independent Agent executions and direct flat-Team executions. | Exact mounted placement identity, immutable run snapshots, launch validation, events, stop, and restore remain. | Topology planner, execution-tree model, runtime docs. |
| BEH-003 | Contract | A handoff is an ordered directional record with one Agent `from`, one Agent-or-Team `to`, and one or more ordered natural-language `rules`; compilation rejects duplicate effective endpoint pairs and self-resolving delivery, and `recipient_address` resolves only within the active root run. Current web authoring does not expose these fields. | AgentOrg/AgentTeam definition surfaces expose one reviewable handoff model with labeled `From`, `To`, and `When` conditions, valid scope-specific endpoint choices, stable ordering, and atomic authoring. | Canonical rooted addresses, Agent-only sources, Agent-or-Team Org destinations, natural-language non-executable conditions, Team coordinator ingress, ordered output, Team-local scope, and failure-closed resolution remain. | Handoff domain/normalizer/compiler, `get_handoff_rules`, package examples, current web absence, and `RIF-AORG-001`. |
| BEH-004 | User / contract | Every AgentTeam, including an organization-like root, requires one direct Agent coordinator; current Team launch opens configuration and preselects its coordinator as focus. | Every flat Team remains coordinator-led, while AgentOrg has no coordinator. AgentOrg `Run` opens one Org configuration surface; launch activates the full Org scope with no initial focus; the user then focuses an exact mounted Agent or Team before a recipient-requiring interaction. | Targeting or focusing a Team still routes through its direct Agent coordinator; missing recipient focus fails closed without first-member or implicit fallback. | Current Team run/configuration/focus behavior plus explicit user feedback in `RIF-AORG-002`. |
| BEH-005 | System | Definition, run, memory, event, history, and frontend contracts preserve recursive configured Team topology. | Persistent configured composition has fixed depth: Org → Team → Agent, with optional independent Agents directly under Org. | Supported content, task results, memory, run identity, and history are preserved when conversion is unambiguous. | Source inventory and server memory TeamRun inventory: 41 at `RER-016`; 43 at the `RER-017` re-probe. |
| BEH-006 | User | Agent Teams UI authors and displays recursively nested Team membership. | Product/API surfaces distinguish AgentOrg from flat Team and prevent unsupported deeper composition. | Standalone flat Teams remain directly definable and launchable. | Web docs/components and existing packages. |
| BEH-007 | Operational | Twenty-three inspected definition roots in the two separately maintained external repositories establish the zero/one-level topology evidence but are not writable migration inputs for this ticket. The original 41 server memory TeamRun trees, and the 43 present at the `RER-017` re-probe, have no deeper configured topology. | This ticket migrates only server-owned definitions and all server memory TeamRun packages. External owners publish target-version Team/Org definitions separately; admitted target definitions preserve the same workflows. | Runtime content/history remains in scope even when its current definition source is external. The external repositories themselves are never rewritten by this ticket. | External ownership clarification, local source ownership, and stored-data re-probe. |
| BEH-008 | Durable contract | Standalone AgentTeam runs use strict `TeamRunExecutionTreeFileV2`: `schemaVersion: 2`, `rootTeam`, and `team_run_execution_tree.json` under the `agent_teams/<rootTeamRunId>` package. Its member/task shapes are generic enough to be reused, but the Team root requires coordinator semantics and currently permits configured Team members. | Preserve that existing Team V2 schema, file name, and physical Team package path for native standalone Teams while narrowing validation to Agent-only configured membership. Add a separate `AgentOrgRunExecutionTreeFileV1` / `agent_org_run_execution_tree.json` under `agent_orgs/<orgRunId>` with `subjectKind: agent_org` and coordinator-free `rootOrg`; reuse the current configured Agent, configured Team, handoff, launch, application-binding, timestamp, and task record shapes. | Flat existing Team V2 bytes and paths remain native; shared child/task semantics, addresses, handoffs, launch state, and run identities remain unchanged across families. | Explicit user correction during the `AD-REV-002` impact round; concrete Team V2 JSON, path/store, domain types, strict schema/validator, builder, history, and stream DTO. |
| BEH-009 | System | Task delegation may create task-scoped Team executions that are structurally nested under the delegating run. | Removing configured Team-within-Team membership does not by itself remove task-scoped delegation to a flat Team. | Supported task creation, settlement, review, and history behavior remain. | Task execution models and imported branch analysis. |
| BEH-010 | Contract / operational | The normal definition codec currently accepts unversioned Team configs with member `refType`, including files mounted from separately maintained package roots. | Server-owned definitions move to explicit target definition versions. External roots are read-only dependencies: normal admission accepts only the exact target Team/Org definition versions and rejects legacy/unversioned packages without modifying them or activating a dual fallback parser. | Compatible package roots continue to participate in catalog/launch/reuse. Rejected external definitions do not block server memory migration or history inspection. | Current provider/package-root contracts, `ARCH-REV-001` recovery, and direct user scope clarification. |
| BEH-011 | User / presentation | The original AgentTeam workspace tree gives a configured Team row its own status signal while preserving individual Agent-row status signals; the current AgentOrg production tree shows Agent dots but omits a status signal on mounted Team rows. | Every direct reusable Team mounted in an AgentOrg shows a presentation-only five-state aggregate status on its Team row, derived from the exact descendant Agent statuses already projected for that Team branch. | Individual Agent status dots, established Team-tree placement/appearance, collapsed-row visibility, exact status authority, and Org-owned lifecycle remain; the aggregate does not create a standalone mounted-Team root or lifecycle. | User-provided original-tree screenshot, production `ORG-005` screenshot, `API-FIND-007`, and the established nested-Team aggregate presentation contract/components. |

## Stakeholders, Actors, And Outcomes

| Actor / Stakeholder | Required Outcome | Important Constraint |
| --- | --- | --- |
| Package author | Compose one Org from independent Agents and reusable flat Teams | Invalid deeper membership fails clearly and never silently flattens. |
| Team developer | Build, launch, and test one AgentTeam independently before adopting it into an Org | Org adoption references the same Team definition rather than requiring a copied or Org-specific Team variant. |
| End user / caller | Configure and launch the complete Org, then focus an exact Agent or Team from the active workspace when communication is needed | AgentOrg is not given a synthetic coordinator, initial recipient, or implicit focus. |
| Org-bound Agent | Use explicit handoffs to reach peers and Teams deterministically | Logical addresses never discover unrelated runs. |
| Existing-data owner | Upgrade the known zero/one-level data without silent identity, content, memory, task, or history loss | Migration may assume there is no deeper configured topology in the relevant existing-data population. |
| External definition owner | Update separately maintained AgentTeam/AgentOrg packages to the published target definition versions | This ticket supplies the server admission contract but does not edit, migrate, commit, or release the external repositories. |
| Product reviewer | Review and approve image-based future-state UI evidence before architecture review resumes | Approved `RV-012` remains authoritative except for the focused `API-FIND-007` correction: mounted Team rows must retain their aggregate status signal without gaining Team-root lifecycle controls. |

## Scope Guardrail (Mandatory)

### In-Scope Use Cases

- `UC-001`: Define and launch one AgentOrg whose direct members are independent Agents and flat Teams.
- `UC-002`: Define, reuse, and independently launch a flat Team containing Agents only and one direct Agent coordinator.
- `UC-003`: Configure and launch the complete AgentOrg without an initial recipient, then focus an exact mounted Agent or Team before messaging or another recipient-requiring interaction; an Org has no coordinator recipient.
- `UC-004`: Address `/agent`, `/team`, and `/team/agent` inside one Org scope and use explicit cross-Team handoffs.
- `UC-005`: Reject Team-in-Team and Org-in-Org configured composition through every supported definition boundary.
- `UC-006`: Convert the server memory Software Development Department and Northstar organization-like run packages into AgentOrg V1 while their separately maintained definition repositories are updated by their owners.
- `UC-007`: Transition the server-owned definition cohort and every server memory TeamRun package present at the cutover boundary; configured topology contains only flat roots or one level of flat child Teams.
- `UC-008`: Update affected API, durable execution, history, and UI contracts so Org and Team semantics are truthful.
- `UC-009`: Preserve supported task-scoped delegation to a flat Team as distinct from configured nesting.
- `UC-010`: Review an image-based product UI prototype covering AgentTeam authoring, AgentOrg authoring, direct-to-Org configuration, full-scope launch, post-launch sidebar focus, and truthful Org-versus-standalone-Team runtime/history presentation before architecture review resumes.
- `UC-011`: Inspect and author Org-owned or Team-local handoffs through explicit `From`, `To`, and `When` fields, including add, edit, delete, reorder, validation, cancel, and atomic definition save.
- `UC-012`: Admit target-version AgentTeam/AgentOrg definition packages from registered external roots, reject incompatible legacy versions with an actionable root/definition diagnostic, and leave those roots unmodified.
- `UC-013`: In active and historical/stopped AgentOrg workspace trees, show an aggregate status signal on every mounted Team row alongside the exact status signals on its Agent descendants, including while the Team row is collapsed.

### Out Of Scope

- Nested AgentOrg, Team-in-Team configured membership, or arbitrary department runtime hierarchy.
- Shared Agent runtime instances mounted in multiple placements.
- Implicit cross-process or unrelated-run routing by logical address.
- Live membership mutation, automatic file watching, or dynamic topology changes.
- A new workflow engine or framework evaluation of natural-language handoff conditions.
- Redesign of task submission/review semantics unrelated to the composition boundary.
- Product Design work unrelated to the AgentOrg/AgentTeam simplification or the identified authoring, configuration/launch/focus, and runtime/history review journeys.
- A broader redesign of the approved `RV-012` workspace, hierarchy, status system, or unrelated Product surfaces while recording the focused mounted-Team status correction.
- Editing, migrating, committing, releasing, or coordinating owner changes inside `/home/autobyteus/workspace/autobyteus-agents` or `/home/autobyteus/workspace/autobyteus-private-agents`.
- A normal-operation dual Team-definition parser or compatibility fallback maintained solely so an external package may remain on its pre-cutover unversioned format.

### Non-Goals

- Adding AgentOrg while retaining recursive AgentTeam composition as an equally supported model.
- Flattening every Agent into one undifferentiated Team and losing Team instructions, coordinator ingress, ownership, or launch scope.
- Renaming the current recursive root Team while keeping its mandatory coordinator and recursion invariants.
- Removing task-scoped Team execution solely because configured Teams become flat.
- Using the requested prototype to reintroduce Team-in-Team authoring, an AgentOrg coordinator, a pre-launch recipient selector, implicit focus/fallback, copied Org-specific Team variants, or unrelated visual redesign scope.
- Treating a mounted Team aggregate indicator as a separately active Team root, persisted Team status, independent `Stop Team` action, or second lifecycle authority inside an AgentOrg.

### Preserved Behavior Boundary

- Preserve canonical rooted addresses, exact mounted placement identity, Team-to-coordinator ingress, Agent-only handoff sources, and same-scope resolution.
- Preserve independently launchable flat Teams and supported Team lifecycle/task behavior, including coordinator ingress for a standalone Team.
- Preserve progressive composition: a proven standalone AgentTeam can be referenced directly by an AgentOrg and connected through Org-scoped handoffs without changing its standalone definition or prior runs.
- Preserve the effective workflows of inspected organization-like packages when their owners publish the target definition format; this ticket does not claim to update those sources.
- Preserve content, memory, tasks, and history while transitioning every server-owned runtime package in the established zero/one-level cohorts, independently of whether its definition source is temporarily admitted.
- Preserve the original Team-row status affordance when a reusable Team is mounted under an AgentOrg: one collapsed-visible aggregate signal on the Team row plus exact individual Agent-row signals, without changing root ownership or lifecycle semantics.

### Review Authority

- Every blocking downstream `Design Impact` or correction must cite an approved requirement, acceptance criterion, or preserved-behavior ID.
- A proposed new product behavior, compatibility promise, migration obligation, or operational policy is a `Requirement Gap` and requires explicit user approval.
- Downstream comments do not amend this package; a scope-changing revision returns to Requirements Engineering and user approval.

## Requirements

| Requirement ID | Requirement | Related Behaviors | Priority | Source / Rationale |
| --- | --- | --- | --- | --- |
| REQ-001 | The product MUST expose `AgentOrg` as the sole persistent composition root that may directly contain independent Agents and flat Teams. | BEH-001, BEH-006 | Critical | User direction; separates organization from Team. |
| REQ-002 | A configured Team MUST contain Agents only and MUST NOT contain a Team or AgentOrg. | BEH-001, BEH-007 | Critical | User direction; removes recursive Team composition. |
| REQ-003 | Every flat Team MUST identify exactly one direct Agent coordinator; targeting the Team MUST deliver through that coordinator. | BEH-004 | Critical | Preserves Team accountability and ingress. |
| REQ-004 | AgentOrg MUST NOT require or invent an Organization coordinator. Invoking `Run` for an AgentOrg MUST open one AgentOrg run-configuration journey without requiring an entry recipient. Launch MUST activate the complete Org scope with no initially focused recipient. After launch, the user MUST focus an exact mounted Agent placement or direct Team placement before sending a message or performing another recipient-requiring interaction; focusing a Team MUST use that Team's coordinator. Launch, configuration, inspection, and non-recipient navigation MUST remain possible without focus, while a recipient-requiring interaction with no valid focus MUST be blocked and MUST NOT guess a first member, Org coordinator, name match, or other fallback. | BEH-002, BEH-004, BEH-006 | Critical | Explicit user correction in `RIF-AORG-002`; separates Org activation from communication targeting without inventing coordinator semantics. |
| REQ-005 | One AgentOrg run MUST own one logical collaboration/address/lifecycle scope whose persistent member addresses are `/agent`, `/team`, and `/team/agent`; `/` is structural and not a recipient. | BEH-002, BEH-003 | Critical | Reuses current simple same-root routing boundary. |
| REQ-006 | A handoff MUST represent one ordered directional route with exactly one Agent `From`, exactly one valid `To`, and one or more ordered `When` conditions. `When` MUST remain trimmed natural-language guidance that the source Agent uses to decide whether the handoff applies; the product MUST NOT present it as executable policy, automatic scheduling, or delivery authorization. AgentOrg-scoped handoffs MUST support a mounted Agent source and a mounted Agent or Team destination, including cross-Team destinations. Team-local handoffs MUST remain scoped to that Team and may be rebased once when mounted. | BEH-003 | Critical | Preserves the existing record/tool semantics while making the user-requested From/To/When model explicit. |
| REQ-007 | Logical `recipient_address` resolution MUST remain within the caller's active AgentOrg or standalone Team scope and MUST fail closed for malformed, root-only, unknown, deeper, or out-of-scope addresses. | BEH-003 | Critical | Prevents accidental global/cross-run routing. |
| REQ-008 | Definition, import/sync, create/update, and launch validation MUST reject Team-in-Team and Org-in-Org input before partial persistence or activation and identify the offending placement. | BEH-001, BEH-006 | High | Makes fixed depth enforceable. |
| REQ-009 | The Software Development Department MUST remain representable as one AgentOrg with direct `requirements_engineer`, `product_design_prototyping_team`, and `software_engineering_team` members and equivalent effective handoffs. | BEH-007 | Critical | User-supplied primary example. |
| REQ-010 | Northstar Operating Company MUST remain representable as one AgentOrg with its independent executive Agents, six direct flat Teams, and equivalent effective handoffs. | BEH-007 | High | Largest inspected real organization example. |
| REQ-011 | Standalone flat Teams MUST remain directly definable, reusable in an AgentOrg, and independently launchable. | BEH-006 | High | Preserves ordinary Team use cases. |
| REQ-012 | Under `PRE-001`–`PRE-005`, migration MUST keep definition-source ownership separate from server runtime ownership. In-scope definition writes are limited to server-owned definitions under the writable server data root and definitions versioned with this implementation repository, including built-in application-owned Teams. The separately maintained `/home/autobyteus/workspace/autobyteus-agents` and `/home/autobyteus/workspace/autobyteus-private-agents` repositories MUST NOT be edited or migrated by this ticket. Every `$MEMORY_ROOT/agent_teams/*/team_run_execution_tree.json` package present at the cutover inventory remains in the runtime cohort regardless of definition origin: direct-Agent roots remain native Team V2 without execution-tree rewrite, while roots with exactly one configured flat-Team level convert to AgentOrg V1 with definition/run identities, direct Team coordinators, addresses, handoffs, launch configuration, content, tasks, memory, and history preserved. | BEH-005, BEH-007, BEH-008, BEH-010 | Critical | Direct user ownership correction, server data/source ownership, stored-run evidence, and the approved two-family runtime boundary. |
| REQ-013 | Migration MUST NOT implement a recursive flattening algorithm or legacy three-or-more-Team-level conversion/compatibility path. An unexpected violation of `PRE-002` MUST stop the affected in-scope migration before mutation and report the invariant. After transition, deeper configured input through import/load/update MUST be rejected by ordinary flat-model validation. Any legacy definition decoder used to convert server-owned sources MUST be migration-only and MUST NOT become the normal external-package parser or fallback. | BEH-001, BEH-007, BEH-010 | High | Keeps migration deterministic and avoids inventing ambiguous semantics or a permanent dual parser for out-of-scope projects. |
| REQ-014 | Native durable state MUST follow the two-family logical/physical contract in `AORG-CONTRACT-001`. Standalone AgentTeam runs MUST retain the existing `TeamRunExecutionTreeFileV2` exact envelope/root keys, `schemaVersion: 2`, `rootTeam`, `team_run_execution_tree.json`, and `$MEMORY_ROOT/agent_teams/<rootTeamRunId>/` package path; only validation narrows `rootTeam.members` to configured Agents. AgentOrg runs MUST use new `AgentOrgRunExecutionTreeFileV1` with `schemaVersion: 1`, top-level `subjectKind: "agent_org"`, coordinator-free `rootOrg`, Org definition/name/run identity, Org default launch configuration, direct Agent-or-Team members, and root-owned task executions, stored at `$MEMORY_ROOT/agent_orgs/<orgRunId>/agent_org_run_execution_tree.json`. The Org file MUST reuse the current V2 timestamp, application-binding, handoff, launch-configuration, configured Agent, configured Team, task Agent, task Team, and task-Team-member field shapes; every configured Team in an Org MUST contain configured Agents only and retain its coordinator. No `TeamRunExecutionTreeFileV3`, generic root-union file, `FlatTeam` subtype, or structural rewrite of native flat Team V2 JSON is authorized. | BEH-008 | Critical | Explicit user correction: keep the existing Team run JSON and add a separate AgentOrg run execution JSON with adjusted Org attributes. |
| REQ-015 | A task Team created by an AgentOrg member MUST be a fresh task-scoped Team execution owned within that AgentOrg run and anchored to the exact delegating host scope. It MUST NOT become a configured Org member or configured child Team. Task delegation from a standalone Team remains owned by that standalone Team run. | BEH-009 | High | Task runtime ownership belongs under the active root scope, while configured membership stays flat. |
| REQ-016 | Affected GraphQL/transport, catalog/detail/launch/history, authoring, and workspace surfaces MUST distinguish AgentOrg from flat Team and MUST stop advertising or accepting configured Team members inside a Team. | BEH-006, BEH-008 | High | Product and external contracts must be truthful. |
| REQ-017 | Concurrent work MUST NOT introduce new recursive configured-Team mutation dependencies. Any future dynamic membership capability requires a separate approved contract and may operate only within the approved Org-direct-member or Team-direct-Agent boundaries. | BEH-001 | High | Reconciles the conflicting draft dynamic-Team branch. |
| REQ-018 | A user MUST be able to create, launch, configure, and test an AgentTeam independently, then add that same Team definition by reference as a direct AgentOrg member and add Org-scoped handoffs. Org adoption MUST NOT require copying the Team, changing its coordinator/internal handoffs, or invalidating its standalone launchability or prior run history. | BEH-001, BEH-006 | High | Enables incremental Team development and simple composition into an Org. |
| REQ-019 | Before `AD-REV-001` proceeds to independent architecture review, a Product Design-owned UI prototype package MUST provide image-based review evidence for four approved journeys: (1) AgentTeam catalog/create-edit/detail with Agent-only membership, coordinator selection, and Team-local handoff inspection/authoring; (2) AgentOrg catalog/create-edit/detail with direct Agents, referenced reusable Teams, no Org coordinator, and Org-owned handoff inspection/authoring; (3) AgentOrg `Run` proceeding directly to one Org configuration surface, launch activating the full Org scope without an initial recipient, and post-launch sidebar focus of an exact Agent or Team before communication; and (4) shared runtime/history presentation that truthfully distinguishes AgentOrg from standalone AgentTeam. The package and its canonical artifact paths MUST receive explicit user approval before they become normative visual evidence. | BEH-001, BEH-003, BEH-004, BEH-006 | High | Explicit user request during `AD-REV-001` review plus `RIF-AORG-001` and `RIF-AORG-002`; validates the coordinated UI/API cutover while preserving no-coordinator/no-fallback semantics. |
| REQ-020 | Handoff inspection and authoring MUST communicate explicit `From`, `To`, and `When` meaning. The exact canonical source/destination addresses MUST be readily inspectable alongside understandable member identity. A Team destination MUST communicate that delivery enters through its named coordinator and make that coordinator's canonical Agent address inspectable. Product Design owns the concrete component, layout, visual prominence, and interaction solution. | BEH-003, BEH-004, BEH-006 | High | Direct user feedback in `RIF-AORG-001`; Requirements Engineering supplies semantic information rather than prescribing the UI. |
| REQ-021 | In an AgentOrg-owned handoff, `From` choices MUST include every configured Agent placement in that Org—direct Agents and Agents inside referenced Teams—and MUST exclude Team, Org, task-scoped, and out-of-scope subjects. `To` choices MUST include direct Agents, Agents inside referenced Teams, and direct referenced Teams, while excluding the Org root, task-scoped subjects, and out-of-scope subjects. In a Team-local handoff, both `From` and `To` choices MUST be direct Agent members of that Team; the Team root is not a destination. | BEH-003, BEH-004 | Critical | Makes fixed-depth endpoint eligibility reviewable without weakening root/address invariants. |
| REQ-022 | Handoff authoring MUST provide a truthful empty state and functional `Add handoff`, `Edit`, `Delete`, and reorder actions. One From/To pair MAY contain multiple ordered `When` conditions, with add/edit/delete/reorder for those conditions. The UI MUST use `handoff` for the directional pair and `When condition` for each natural-language rule, and MUST NOT label a count of endpoint pairs as “rules.” Draft actions MUST remain reversible until the parent definition is saved. | BEH-003, BEH-006 | High | Resolves the inert and ambiguous “Add rule” interaction identified by the user. |
| REQ-023 | The complete Org or Team definition draft MUST validate and save atomically. Save MUST be blocked with field- and handoff-specific feedback when From/To/When is missing, an address is invalid or no longer mounted, the source/destination is ineligible, delivery resolves back to the source Agent, or an effective From/To pair duplicates another handoff. Member removal or rename MUST NOT silently delete, retarget, or leave stale handoffs; the author must resolve every affected handoff before save. Cancel MUST discard the handoff draft, and save success/failure MUST be visible without losing a failed draft. Org create/edit MUST edit only Org-owned handoffs; Team-local handoffs MUST remain editable only from that Team's surface. Any compiled/effective view that combines scopes MUST label each handoff's owner and remain read-only. | BEH-001, BEH-003, BEH-006 | Critical | Preserves validate-before-write, Team reuse, and scope ownership while defining the requested complete authoring lifecycle. |
| REQ-024 | One AgentOrg run-configuration journey MUST seed the Org root from the AgentOrg definition's launch defaults and apply the resulting root choices across the complete mounted scope unless the user records a more specific placement override. For an Agent inside a referenced Team, effective supported run settings MUST resolve in this order: exact Agent placement override, containing Team placement override, Org root choices. For a direct Org Agent, the exact Agent placement override takes precedence over Org root choices. Referenced Team and Agent definition defaults MUST NOT independently replace the Org root choices merely because those definitions are mounted; those defaults remain available for their standalone launch journeys. Org-run overrides MUST NOT mutate a referenced Team or Agent definition, and Team focus/coordinator ingress MUST remain independent of configuration inheritance. Launch MUST validate complete effective configuration for every mounted execution before activating the Org. | BEH-002, BEH-004, BEH-006 | Critical | Preserves the current Team configuration-first/default-and-placement-override model at the approved fixed depth while keeping reusable definitions and Team ingress intact. |
| REQ-025 | Any history, catalog, restore-location, GraphQL, stream, workspace, or other API that can return both root kinds MUST use an explicit discriminated union with logical discriminator `root_subject_kind: "agent_team" \| "agent_org"`. The `agent_team` variant MUST project the unchanged Team V2 root/file family; the `agent_org` variant MUST project AgentOrg V1. Readers MUST select the persisted validator from the known package family/path plus its strict schema version/root keys and MUST NOT infer root kind from coordinator presence, display name, member shape, or version alone. Existing Team-only contracts MAY remain Team-specific and byte/wire compatible. | BEH-005, BEH-006, BEH-008 | Critical | Two physical persistence families require truthful, failure-closed generic discovery and projection without forcing changes onto Team-only clients. |
| REQ-026 | Normal definition admission MUST accept exactly `AgentTeamDefinitionConfigFileV2` (`team-config.json`, numeric `schemaVersion: 2`, Agent-only `members` with no `refType`, exactly one direct-Agent coordinator) and `AgentOrgDefinitionConfigFileV1` (`org-config.json`, numeric `schemaVersion: 1`, explicit Agent-or-Team member `refType`, no coordinator), as defined in `AORG-CONTRACT-001`. An unversioned, wrong-version, recursive, unresolved, or family-mismatched definition MUST be rejected with its package root, definition identity/path, expected version, and reason. Normal catalog/launch/authoring MUST NOT try the retired definition shape after target admission fails. | BEH-001, BEH-006, BEH-010 | Critical | Makes the server-owned target codec and external-project publication contract explicit without a dual normal parser. |
| REQ-027 | The two external definition repositories MUST be treated as read-only dependencies and updated in separate owner work. At server cutover, a compatible definition is admitted normally; an incompatible external definition and any Org that depends on its unavailable Team MUST be excluded from new-run catalog/launch/authoring with an actionable diagnostic until its owner publishes the target version. That per-definition temporary unavailability MUST NOT block server startup, compatible definitions, server memory migration, or history/inspection of existing run snapshots. The server may reject the retired external format only after the target codecs, diagnostics, server-owned definition conversion, and runtime migration gate are ready. | BEH-006, BEH-007, BEH-010 | Critical | User-approved availability boundary following the explicit external ownership correction. |
| REQ-028 | Every direct configured Team placement shown inside an AgentOrg execution hierarchy MUST display a Team-row aggregate status indicator in addition to every descendant Agent's exact status indicator. The Team aggregate MUST fold only the Agent execution statuses already projected inside that Team branch—including configured Agents and task-scoped descendant Agents—using precedence `running > initializing > error > idle > offline`; the Team container, direct Org Agents, ancestors, sibling Teams, and rows outside the branch MUST NOT contribute. Unknown, missing, or empty input MUST be `offline`. Active views MUST react to their authoritative live/current Agent-row projection; historical or stopped views MUST use their truthful historical/terminal Agent-row projection and MUST NOT retain stale `running` or `initializing` when no live authority exists. The indicator MUST occupy the established status position on the Team row, remain visible when the Team is collapsed, expose its meaning without color alone, and preserve the original Team hierarchy appearance as far as the fixed-depth Org model allows. It is presentation-only: it MUST NOT be persisted or transported as independent Team state, create Team-root ownership, enable a mounted-Team Stop/restore/archive action, or influence focus, readiness, routing, command admission, or Org lifecycle. | BEH-006, BEH-011 | Critical | Explicit user correction in `API-FIND-007`; preserves the established five-state configured-Team aggregate while keeping AgentOrg as the sole lifecycle root. |

## Acceptance Criteria

| AC ID | Related Requirements | Trigger | Observable Expected Outcome | Important Failure / Alternate Outcome |
| --- | --- | --- | --- | --- |
| AC-001 | REQ-001–REQ-003 | Author an Org with one independent Agent and two flat Teams | Definition is valid; each Team contains Agents only and has one direct Agent coordinator | A Team containing a Team/Org is rejected before persistence. |
| AC-002 | REQ-004 | From AgentOrg detail, invoke `Run`, complete configuration, launch, then try to message before and after choosing a workspace-sidebar member | `Run` goes directly to AgentOrg configuration; launch activates the complete Org scope with no focused recipient; the active workspace is usable for non-recipient inspection; selecting an exact Agent focuses that Agent and selecting a Team focuses its coordinator for communication | A separate pre-launch entry selector, Org coordinator, automatically focused first member, name match, or implicit fallback is not created; messaging with no valid focus is blocked and asks for an exact sidebar selection. |
| AC-003 | REQ-005–REQ-007 | An Org-bound Agent uses collaboration tools | That Agent receives its outgoing `When` conditions in persisted order; `/agent`, `/team`, and `/team/agent` resolve exactly in the active Org; `/team` targets its coordinator | The framework does not execute/evaluate condition prose, and `/`, malformed/deeper/unknown/out-of-scope destinations fail closed. |
| AC-004 | REQ-006, REQ-009, REQ-027 | After its external owner publishes a target-version Software Development Department AgentOrg definition, admit and run it against the converted server runtime/history model | Existing Requirements ↔ Product Design/Engineering/Delivery workflow reaches the same effective recipients | No Team-inside-Team definition is required, and this ticket does not edit the external source repository. |
| AC-005 | REQ-002, REQ-008 | Import/edit a Team containing `refType=agent_team` or Org | The operation identifies the invalid member and performs no partial save/activation | It does not silently flatten or mount the member elsewhere. |
| AC-006 | REQ-010, REQ-027 | After its external owner publishes a target-version Northstar AgentOrg definition, admit and run it under the fixed-depth model | All direct executives, six Teams, and current cross-Team destinations fit addresses no deeper than `/team/agent` | Unsupported deeper or retired-format input follows admission/AC-005 validation rather than in-ticket external migration. |
| AC-007 | REQ-011 | Define and launch a flat Team without an Org, then reuse it in an Org | Standalone coordinator-led collaboration works; the same definition can be mounted as a direct Org Team | Org membership is not mandatory for ordinary Team work. |
| AC-008 | REQ-012, REQ-013 | Under `PRE-001`–`PRE-005`, transition all server-owned definition sources and every server memory TeamRun package present at the cutover inventory | Server-owned flat definitions publish Team Definition V2; any server-owned organization-like definition publishes Org Definition V1. Every server runtime root classifies as flat or one-level organization-like: the former remains at its Team V2 execution file/path unchanged, while the latter converts to AgentOrg V1 with supported state and task executions attached to the same semantic host. The external public/private repositories receive no writes. | The affected in-scope item fails before writes if configured depth or mapping violates a precondition; no deep compatibility path, Team execution V3 rewrite, or external-repository migration is attempted. |
| AC-009 | REQ-014 | Persist and restore one new standalone AgentTeam run and one new AgentOrg run, then reopen an existing flat Team V2 run | The new and existing Teams use exact Team V2 `schemaVersion/rootTeam/file/path` with Agent-only configured membership; the Org uses exact AgentOrg V1 `subjectKind/rootOrg/file/path`, no Org coordinator, direct Agent/flat-Team members, Org default configuration, and reused V2 child/handoff/task shapes; both restore with the same identities and content | Strict validation rejects a Team/Org file in the wrong package family, wrong schema/root keys, a coordinator on `rootOrg`, a Team member beneath a Team, a missing Team coordinator, or unsupported configured depth. |
| AC-010 | REQ-015 | An Org member delegates a task to a flat Team | A fresh task TeamRun starts through that Team's coordinator, is recorded under the same AgentOrg execution aggregate at the delegating host scope, and settles/reports under supported task semantics | It is not added to the Org definition/member list and receives no new permanent Org member address. A task delegated from a standalone Team remains under that Team run. |
| AC-011 | REQ-016 | Use affected API and web authoring/launch/history flows | Org and Team roles are distinguishable; Team authoring offers Agent members only; existing flat-Team flows remain usable | Nested-Team selectors/counts/warnings are absent from flat-Team authoring. |
| AC-012 | REQ-017 | Reconcile the dynamic-AgentTeam draft work | No new production path can add/remove a configured Team beneath a Team | Reusable dynamic ideas, if any, require a separately approved fixed-depth contract. |
| AC-013 | REQ-018 | A tested standalone AgentTeam is added by reference to an AgentOrg and Org-level handoffs are authored to/from it | The Org validates and launches using the same Team definition identity, coordinator, Agent membership, and Team-local handoffs; Org handoffs reach the Team/coordinator or its Agents; the Team remains independently launchable and its earlier runs remain intact | No copied Team definition, Org-specific Team subtype, or configured Team nesting is introduced. |
| AC-014 | REQ-019 | The Product Design package is presented for user review before architecture review resumes | Final image references and the Product Design-owned specification/review record cover all four journeys, agree on the same accepted revision and durable paths, visibly preserve Agent-only Team membership, Team coordinator selection, referenced reusable Teams, a coordinator-free Org, direct-to-Org configuration, full-scope launch with no initial recipient, exact post-launch sidebar focus, truthful handoff detail/authoring, and truthful root-kind runtime/history presentation, and carry an explicit user-approval reference | Missing journey/handoff images, disagreement between artifacts, recursive Team authoring, an Org coordinator, a separate pre-launch entry selector, an implicit focus/recipient fallback, an inert authoring control, or absent user approval keeps architecture review on hold and returns the package for Product Design/requirements reconciliation. |
| AC-015 | REQ-006, REQ-020 | Inspect a Team-local or Org-owned handoff | The exact `From`, `To`, and every `When` condition are unambiguous; member identity and canonical addresses are readily inspectable; a Team `To` clearly conveys coordinator delivery with the named coordinator and its Agent address | A generic “Agent → coordinator” summary, inaccessible endpoint identity, condition nickname that omits the actual guidance, or ambiguous “N rules” count does not satisfy the requirement. |
| AC-016 | REQ-021 | Add or edit an Org-owned handoff | `From` offers the Org's direct Agents and mounted Team Agents only; `To` offers direct Agents, mounted Team Agents, and direct Teams; choices are grouped or otherwise distinguish scope and show exact addresses | Org/Team roots as sources, Org root as destination, task executions, unrelated subjects, direct Team-as-source, and self-resolving or duplicate effective endpoints are unavailable or produce blocking inline validation. |
| AC-017 | REQ-022, REQ-023 | Starting from zero or existing handoffs, add one route with two When conditions, edit it, reorder both conditions and handoffs, delete another route, then save | The empty/add/edit/delete/reorder controls are functional and keyboard reachable; the saved definition preserves endpoint and condition order exactly; success is announced; reopening shows the saved order | Cancel leaves the persisted definition unchanged; invalid or failed save identifies affected fields/handoffs, preserves the draft, and performs no partial mutation. |
| AC-018 | REQ-018, REQ-023 | Edit Org handoffs involving an Agent inside a referenced Team, then open that Team's handoff editor | Org editing changes only Org-owned routes; the referenced Team retains the same definition and Team-local handoffs; Team-local `From`/`To` choices contain only that Team's direct Agents | Org editing never mutates Team-local rules. A combined effective/runtime view, if supplied, is owner-labeled and read-only rather than a second editing authority. |
| AC-019 | REQ-004, REQ-024 | Open one Org configuration, change an Org-root value, customize one referenced Team placement and one Agent placement, then launch | Direct Org Agents inherit the Org-root choices unless specifically overridden; Team Agents inherit their Team placement choices unless specifically overridden; the exact Agent override wins for that Agent; launch activates only after every mounted execution has a valid effective configuration | Referenced definition defaults do not silently replace the Org choices, Org-run edits do not mutate reusable Agent/Team definitions, and changing configuration does not choose a recipient or alter Team coordinator ingress. |
| AC-020 | REQ-016, REQ-025 | List, stream, or open mixed AgentTeam and AgentOrg history through a generic surface | Every item/event carries `root_subject_kind`; `agent_team` selects/projects Team V2 and `agent_org` selects/projects AgentOrg V1, while existing Team-only clients keep their Team-specific contract | No consumer guesses kind from coordinator, name, members, schema version alone, or whichever file happens to parse; a family/discriminator mismatch fails closed. |
| AC-021 | REQ-026 | Admit one valid Team Definition V2 package and one valid Org Definition V1 package, then submit unversioned, wrong-version, recursive, unresolved, and family-mismatched examples | Valid packages appear under the correct Team/Org subject and remain reusable. Every invalid package is excluded before catalog/launch activation and reports root, path/identity, expected version, and reason. | Normal admission never retries a retired/unversioned definition parser, silently inserts/removes `refType`, or mutates the source package. |
| AC-022 | REQ-012, REQ-027 | Start/cut over with one compatible external Team, one incompatible external Team, an Org referencing the incompatible Team, server-owned target definitions, and the server memory runtime cohort | The compatible external Team and server-owned definitions remain available; the incompatible Team and dependent Org are unavailable for new-run catalog/launch/authoring with actionable diagnostics; server memory migration and existing history/inspection complete; neither external repository changes. | Server startup/readiness is not globally blocked by incompatible external definitions, and no unavailable definition is activated through fallback. Separate owners can restore availability by publishing exact target versions. |
| AC-023 | REQ-028 | Open an active AgentOrg containing a mounted Team whose descendant Agents span multiple statuses; inspect the Team expanded and collapsed, then inspect the stopped/historical view | The Team row shows one accessible aggregate status using `running > initializing > error > idle > offline`, each Agent retains its exact own status indicator, the Team signal remains visible when collapsed, and the stopped/historical signal follows the truthful projected descendant states without stale live activity | Direct Org Agents, sibling Teams, and containers do not affect the aggregate; no mounted-Team Stop/restore/archive control, Team-root persistence, independent lifecycle state, polling authority, or loss of the individual Agent signals is introduced. |

## Relevant Scenarios And Journeys

| Scenario ID | Kind | Starting Condition / Sequence | Expected Outcome | Related IDs |
| --- | --- | --- | --- | --- |
| SCN-001 | Contract | Define Org members and Org-scoped handoffs; validate | One fixed-depth Org definition is accepted | REQ-001–REQ-008; AC-001–AC-003 |
| SCN-002 | User / system | From Org detail, invoke `Run`; configure the Org; launch the full scope with no initial focus; select an Agent or Team from the workspace sidebar before messaging; persist/restore | One Org-owned scope activates and retains exact identities without an Org coordinator; recipient-requiring interactions use only the exact post-launch focus and Team focus enters through its coordinator | REQ-004, REQ-005, REQ-014; AC-002, AC-009 |
| SCN-003 | Contract | An Agent reviews its ordered natural-language `When` conditions and applies a handoff that targets a peer Team | Destination resolves in the same Org and reaches the Team coordinator; the framework does not evaluate the prose | REQ-003, REQ-006, REQ-007; AC-003 |
| SCN-004 | Operational | Upgrade server-owned definitions and inventory every server memory TeamRun root while leaving separately maintained external package roots read-only | Preserve flat runtime roots in native Team V2 unchanged; convert only one-level organization-like runtime roots to AgentOrg V1; publish target versions for server-owned definitions; preserve supported runtime state | REQ-012–REQ-014; AC-008, AC-009 |
| SCN-005 | User | Launch a standalone Team and delegate work to a Team | Flat Team and task-scoped execution remain usable | REQ-011, REQ-015; AC-007, AC-010 |
| SCN-006 | User | Create and repeatedly test a standalone AgentTeam, then reference it from an AgentOrg and add Org-scoped handoffs | The same Team becomes a direct Org member without losing standalone behavior or history | REQ-011, REQ-018; AC-007, AC-013 |
| SCN-007 | User | Review the requested image-based prototype across Team catalog/authoring/detail, Org catalog/authoring/detail, direct-to-Org configuration/full-scope launch/post-launch sidebar focus, and shared runtime/history | The user approves one coherent UI model that visibly preserves the approved AgentOrg/AgentTeam, handoff, and launch/focus semantics before architecture review resumes | REQ-019; AC-014; explicit request during `AD-REV-001` review, `RIF-AORG-001`, `RIF-AORG-002`, approved `RV-012`, and `VIS-001`–`VIS-020` |
| SCN-008 | User | Inspect, add, edit, delete, and reorder explicit From/To/When handoffs; change a referenced member; cancel once and atomically save once | Scope-eligible choices, canonical identities, Team coordinator ingress, condition order, member-impact validation, ownership separation, cancellation, and save feedback are all reviewable without mutating a referenced Team | REQ-006, REQ-020–REQ-023; AC-015–AC-018; `RIF-AORG-001` |
| SCN-009 | User | In one AgentOrg configuration journey, review Org-root choices, customize a Team placement and an Agent placement, launch, then select a member from the active workspace | Effective configuration follows Org → Team → Agent specificity, reusable definition defaults/identity remain unchanged, no recipient is selected by configuration, and Team focus still enters through its coordinator | REQ-004, REQ-024; AC-002, AC-019; current AgentTeam configuration behavior and `RIF-AORG-002` |
| SCN-010 | System / contract | A combined history/stream/workspace reader discovers both native Team V2 packages and AgentOrg V1 packages | It returns an explicitly discriminated `agent_team`/`agent_org` union, applies the correct strict validator/projection for each family, and preserves Team-only compatibility | REQ-014, REQ-016, REQ-025; AC-009, AC-020; current Team store/path/DTO plus explicit durable-family correction |
| SCN-011 | Operational / contract | Server cutover scans server-owned and registered external definition roots containing target and retired package versions | Target Team V2/Org V1 definitions are admitted; incompatible external definitions and their dependent Orgs are diagnosed and unavailable for new work without blocking compatible definitions, server memory migration, or existing history; external sources are unchanged | REQ-012, REQ-013, REQ-026, REQ-027; AC-021, AC-022 |
| SCN-012 | User | In an AgentOrg workspace/history tree, inspect a mounted Team while its Agent descendants change status, collapse the Team, and later inspect the stopped run | The mounted Team retains a truthful, collapsed-visible aggregate status signal alongside exact Agent-row signals, while Stop/restore and root ownership remain exclusively AgentOrg-level | REQ-016, REQ-028; AC-011, AC-023; user `API-FIND-007` clarification and original/current browser screenshots |

## UI, Interaction, And Experience Requirements

- Applicable: `Yes`; current authoring, launch, detail, workspace, and history surfaces expose recursive Teams.
- Product Design & Prototyping request: `Explicit — after the original image-based prototype request, the user asked that the original Team-row status signal be restored for Teams mounted in an AgentOrg through a focused supplement/reference revision without reopening unrelated RV-012 behavior`; this request is satisfied by completed `AORG-FLAT-TEAM-STATUS-001`.
- Product Design outcome: `Prototype Completed — RV-012`, subsequently promoted to the normal/default prototype baseline by completed `BASELINE-PROMOTION-001`; focused mounted-Team status supplement `AORG-FLAT-TEAM-STATUS-001` is also `Prototype Completed` and user-approved. Product reports no remaining requirement impact or Product UI decision.
- User-supplied screenshots: the original AgentTeam tree is authoritative evidence for the Team-row status affordance and placement; the current production `ORG-005` capture proves the omission. The focused Product references now provide normative active-expanded, active-collapsed, and stopped/historical evidence.
- Approved review decision: one clear visual and interaction model for (a) reusable Agent-only, coordinator-led Teams; (b) coordinator-free Orgs that reference Agents and Teams; (c) AgentOrg `Run` going directly to one Org configuration journey, full-scope launch without a recipient, and exact post-launch Agent/Team focus from the workspace sidebar; and (d) a shared runtime/history experience that distinguishes the root subject truthfully.
- Normative interaction constraints carried into Product Design: no Team-in-Team or Org-in-Org authoring; no Org coordinator, pre-launch entry selector, initial focus, or implicit recipient fallback; Org configuration applies root defaults with Team/Agent placement overrides without mutating referenced definitions; a referenced Team retains its identity, Agent membership, coordinator, Team-local handoffs, standalone launchability/defaults, and prior history; task-scoped Team lineage must not be presented as configured nesting.
- Approved review coverage: AgentTeam catalog/create-edit/detail; AgentOrg catalog/create-edit/detail; direct-to-Org configuration, full-scope launch, and post-launch sidebar focus; shared active-runtime and history presentation.
- Product Design requirement-impact findings: `RIF-AORG-001` at `RV-006` found that condensed handoff cards and an inert `Add rule` action do not make the handoff contract reviewable; its semantic/capability clarification was approved in `RER-012`. `RIF-AORG-002` records the user's explicit rejection of `RV-009`'s separate pre-launch entry selector and approved direction toward direct Org configuration, full-scope launch, and post-launch sidebar focus in `RER-013`. Downstream `API-FIND-007` records the user's explicit correction that the approved package omitted the established mounted-Team aggregate status signal; `AORG-FLAT-TEAM-STATUS-001` resolves the Product evidence gap with no further requirement impact.
- Information Product Design must represent: one directional **Handoff** has one Agent `From`, one eligible `To`, and one or more ordered natural-language **When conditions**; exact canonical endpoint identity is inspectable; a Team destination enters through its named coordinator; counts must not conflate endpoint pairs with conditions.
- Capabilities Product Design must enable: add/edit/delete/reorder handoffs and their When conditions; choose scope-eligible endpoints; work in a reversible definition draft; validate the complete definition; cancel; and save atomically with understandable success/failure feedback.
- Ownership information Product Design must preserve: Org surfaces change Org-owned handoffs only. Team-local handoffs remain owned by and editable from that Team. Org choices may reach mounted Team Agents and direct Teams, but editing the Org never edits the referenced Team. Org-run configuration similarly changes only the run draft, not the reusable Agent/Team definitions. Any combined effective/runtime handoff list is owner-labeled and read-only.
- Launch/configuration/focus information Product Design must preserve: AgentOrg `Run` opens one Org configuration journey; Org-root choices apply across the scope, Team placement overrides apply to that Team and its Agents, and exact Agent overrides apply last; launch activates the full Org with no focus; the workspace sidebar supplies the exact Agent or Team focus before communication; Team focus uses its coordinator; no separate entry selector or implicit focus exists.
- Mounted-Team status information Product Design must represent: every configured Team row in an AgentOrg shows one aggregate status in the established pre-icon status position while every Agent row keeps its own status; the Team signal remains visible when collapsed and communicates `running`, `initializing`, `error`, `idle`, or `offline` without color alone. The visual treatment must not imply an independent Team root, lifecycle, or Stop action.
- Product Design authority: Product Prototyper owns the concrete UI structure, layout, component choice, visual hierarchy, interaction treatment, prototype revisions, and final visual recommendation, within the approved semantic and observable-behavior constraints.
- Accessibility baseline: handoff fields/errors/actions and ordering remain keyboard/screen-reader operable; status/error feedback is announced; visual identity is not color-only. A mounted Team's aggregate dot and containing tree item must expose the Team identity and aggregate state through accessible text/title semantics even when descendants are collapsed.
- Approved Product UI authority: `/home/autobyteus/workspace/autobyteus-web-prototype/tickets/done/AORG-FLAT-TEAM-001/ui-ux-spec.md` and `VIS-001`–`VIS-020` remain normative for the cumulative experience. `/home/autobyteus/workspace/autobyteus-web-prototype/tickets/done/AORG-FLAT-TEAM-STATUS-001/ui-ux-spec.md` and `VIS-STATUS-001`–`VIS-STATUS-003` are the focused normative supplement for `REQ-028` / `AC-023` and supersede only the prior `API-FIND-007` omission. Unrelated visible decisions remain unchanged.
- Default-baseline activation authority: `/home/autobyteus/workspace/autobyteus-web-prototype/tickets/done/BASELINE-PROMOTION-001/ui-ux-spec.md`, `promotion-audit.md`, and `visual-references` prove that the accepted experience is now reachable through normal navigation and clean URLs without `prototypeReview` state. Historical review URLs remain evidence-only; this promotion does not replace or change the normative `RV-012` UI behavior.
- Approved experience evidence includes Team and Org catalog/authoring/detail, Team-local and Org-owned From/To/When handoffs without visible Handoff or condition ordinals, direct-to-Org configuration with placement overrides, full-scope unfocused activation, exact post-launch Agent/direct-Team focus, Team coordinator ingress, production-style runtime hierarchy/task lineage, desktop/narrow states, and the mounted-Team aggregate signal in active-expanded, active-collapsed, and stopped/historical states. Product-owned mocked boundaries remain non-production: persistence, services, credentials, orchestration, streams, permissions, concurrency, and production writes are not demonstrated by the prototype.

## Quality And Non-Functional Requirements

| Quality ID | Area | Requirement | Verification Intent |
| --- | --- | --- | --- |
| QR-001 | Reliability | Invalid membership and addresses fail closed without name, coordinator, or flattening fallback. | Negative contract/runtime tests. |
| QR-002 | Compatibility | Both server runtime cohorts and each definition-source ownership/admission class have an explicit, repeatable, test-covered outcome. | Transition/admission matrix and idempotency tests. |
| QR-003 | Operability | An unexpected deeper input fails as an invariant/validation error before mutation rather than activating a legacy compatibility path. | Negative migration and validation tests. |
| QR-004 | Integrity | Restore uses the durable run snapshot and preserves unambiguous placement, content, task, memory, and history identity. | Persistence/restore tests. |
| QR-005 | Simplicity | Supported configured composition depth is exactly Org → Team → Agent, with independent Agents directly under Org. | Schema/topology invariant tests. |
| QR-006 | Accessibility | Handoff fields, choices, validation, actions, ordering, and save status are operable and understandable without pointer-only input or color-only meaning. | Keyboard/screen-reader browser validation, including reorder and error recovery. |
| QR-007 | Operability | One incompatible external definition cannot block compatible definitions, server memory migration, or history inspection; its diagnostic identifies the external root, definition/path, expected version, and separate owner action. | Mixed-version registered-root startup/catalog/launch tests. |
| QR-008 | Accessibility / truthfulness | Mounted Team aggregate status is perceivable without color alone, stays available when descendants are collapsed, and never communicates independent Team lifecycle ownership. | Desktop/narrow browser and screen-reader/name-role-state validation across active, collapsed, stopped, and missing-status states. |

## Data Continuity And Acceptable Loss

- Persisted or external data affected: `Yes`.
- Must preserve: unambiguously mapped definition identity, member placement, Team coordinator, handoff endpoint, Agent/Team run identity, communication/task content, memory reachability, and history.
- Acceptable rebuild: derived catalogs, indexes, UI caches, and regenerated projections.
- Not acceptable: silent deletion, endpoint truncation/rebasing, or identity/content/history loss during either established cohort transition.
- Not acceptable under the corrected durable boundary: version-bumping, renaming, moving, or structurally rewriting an already-flat native Team V2 execution tree merely to place it in a generic root family.
- Definition ownership: the 23 inspected roots in `/home/autobyteus/workspace/autobyteus-agents` and `/home/autobyteus/workspace/autobyteus-private-agents` are external evidence/dependencies, not this ticket's write/migration cohort. Observed server-owned production definition sources are one writable data-root Team and two built-in application-owned Teams; cutover inventories the actual server-owned set.
- Runtime volume: 41 readable server memory TeamRun trees at `RER-016`; the `RER-017` re-probe found 43 (27 flat, 16 with one configured Team level, none deeper). Every server memory package present at the cutover inventory is in scope, including runs whose definition source is external.
- Approved topology assumption: the in-scope server runtime population contains no deeper configured topology. Deep input encountered later is unsupported input, not a migration cohort. External definition sources retain topology-evidence value but are converted only by their separate owners.

## Migration Preconditions / Preknown Conditions

These conditions are authoritative inputs to future migration design and implementation, not questions that must be re-investigated unless contradictory data is actually produced.

| Precondition ID | Established Condition | Consequence For Migration | Evidence / Authority |
| --- | --- | --- | --- |
| PRE-001 | Definition-source ownership and server runtime ownership are separate. This ticket may write only server-owned definitions under the writable server data root or this implementation repository; the public/private external definition repositories are read-only dependencies and separate owner work. Every server memory TeamRun package at cutover remains runtime-migration input regardless of definition origin. | Build separate definition-source and runtime inventories; never infer permission to write an external source from a stored run's definition ID. | Direct user ownership clarification, package-root provider model, and filesystem/source inventory. |
| PRE-002 | In-scope server definitions and server memory runtime snapshots, plus the external packages used as topology evidence, have at most one configured Team-membership edge: root Team → optional direct child flat Team → Agents. No inspected runtime has root Team → child Team → grandchild Team data. | Runtime migration and server-owned definition conversion may be fixed-depth and MUST NOT implement recursive/deep flattening logic. | Original 23 external definition roots; 41 stored trees at `RER-016`; 43-tree `RER-017` re-probe; explicit user confirmation. |
| PRE-003 | The server memory runtime population has exactly two configured cohorts: Agent-only roots and organization-like roots with direct flat child Teams. The definition-source population instead splits by ownership/admission: server-owned sources are convertible here; external sources must independently publish target versions. | Agent-only runtime roots remain native Team V2 without execution-tree migration; organization-like runtime roots convert to AgentOrg V1. Do not treat the 23 external definition roots as migration writes. | Stored-run inventory, source ownership, and corrected durable-family direction. |
| PRE-004 | Northstar and Software Development Department external definitions are examples/evidence for the one-level AgentOrg target, while their server memory TeamRun packages are in-scope runtime records. | Convert the server memory roots here; their external owners separately publish AgentOrg Definition V1 and flat Team Definition V2 packages before those definitions can be admitted for new runs. | Package configs, screenshot, server memory packages, Architecture Review recovery, and user clarification. |
| PRE-005 | Task-scoped Team executions are runtime/task lineage, not configured Team membership depth. | Preserve and reattach task execution records under the correct Org/Team host scope; do not interpret them as a third configured composition level. | Current task execution contract and user task-Team clarification. |

If a migration probe unexpectedly contradicts `PRE-002`, the affected in-scope migration fails before writes and reports the violated invariant. An incompatible external package follows the admission boundary rather than becoming a migration target. Neither case authorizes a deep legacy conversion path or normal dual parser.

## External Contracts And Dependencies

| Contract / Dependency | Required Outcome | Evidence / Risk |
| --- | --- | --- |
| Server-owned definition format | Publish exact Team Definition Config V2 and AgentOrg Definition Config V1; migrate only writable server-owned definitions | Current server/data and built-in application Team configs are unversioned and include member `refType`; a migration-only legacy decoder may convert them before target-only admission. |
| External definition roots | `/home/autobyteus/workspace/autobyteus-agents` and `/home/autobyteus/workspace/autobyteus-private-agents` are read-only dependencies for this ticket | Separate owners must publish exact target definition versions; normal server admission rejects incompatible definitions without fallback or source mutation. |
| GraphQL/stream/web contracts | Expose truthful Org/Team identity and fixed-depth composition | Current types and UI recursively expose Teams. |
| Collaboration tools | Preserve canonical same-scope addresses, ordered Agent handoffs, and Team coordinator targeting | Current logical routing does not join independent runs. |
| Durable execution/history | Keep native Team V2 schema/file/path unchanged for flat Teams; add exact AgentOrg V1 schema/file/path; reuse existing child/handoff/task shapes; discriminate mixed projections explicitly | User rejected the generic V3 root union during the `AD-REV-002` impact round; current Team store/path/DTO are strict and Team-specific. |
| Remote flat-team bootstrap | Incorporated as evidence and concept input at commit `c3a318812` (cherry-picked as `ca6d24dfa`) | Its no-Org-coordinator and task-scope distinction are adopted; its duplicate draft is not canonical. |
| Dynamic-AgentTeam draft branch | Must not add recursive configured-Team mutation | `origin/codex/dynamic-agent-team-runtime@7d9b4ba69` conflicts with the target if implemented as drafted. |

## Supplemental Artifacts

| Artifact | Purpose | Status / Approval Applicability |
| --- | --- | --- |
| `tickets/in-progress/flat-agent-organization-model/agent-org-contract.md` | Single configured-structure, definition admission/ownership, launch/configuration/focus, handoff, mounted-Team status presentation, two-family on-disk execution, task anchoring, generic projection, and migration contract | `Approved — RER-021`; `ORG-CASE-056`–`ORG-CASE-058` are supported by the user-approved focused Product supplement. |
| `tickets/in-progress/flat-agent-organization-model/investigation-notes.md` | Canonical evidence base | Current; supports but does not independently define behavior. |
| User screenshot at the absolute path recorded in investigation notes | Current structure evidence | Evidence only. |
| `/home/autobyteus/workspace/autobyteus-web-prototype/tickets/done/AORG-FLAT-TEAM-001/ui-ux-spec.md` | Approved normative UI/UX supplement for `REQ-019` / `AC-014` and the affected Team/Org, handoff, configuration, focus, runtime, history, responsive, and accessibility behavior | `Approved`; Product revision `RV-012`, runnable behavior commit `891b4e667d51fffb92ebd85dd2dc90a8e0afcf2e`, explicit user approval in `user-decision-record.md`. |
| `/home/autobyteus/workspace/autobyteus-web-prototype/tickets/done/AORG-FLAT-TEAM-001/visual-references` and `visual-reference-manifest.json` | Normative final image set, route/state/viewport inventory, hashes, and fixture boundaries | `Approved`; `VIS-001`–`VIS-020`, captured after approval at commit `3de2c08b6f3d8cfdb75714edaa88b00d04d67aaf`; manifest reports 59 checks, zero failures, and zero runtime errors. |
| `/home/autobyteus/workspace/autobyteus-web-prototype/tickets/done/AORG-FLAT-TEAM-001/user-decision-record.md`, `prototype-ticket.md`, and `validation/final-package` | Explicit user decision, terminal Product ticket identity, provenance, completed validation, and repository finalization evidence | `Completed`; Product package/integration revision `e53b8acbbc714a2571142a297fcba59f1b0818e5`, terminal closure `79093e9a781024844fe3d3678e93ab44386c1040`, final integrity 6/6 pass. |
| `/home/autobyteus/workspace/autobyteus-web-prototype/tickets/done/BASELINE-PROMOTION-001` | Default-baseline promotion record, UI/UX activation supplement, audit, clean-entry references, validation, and finalization | `Completed`; user-authorized activation only, promoted behavior `bdf071b8cc6819206f0c8768198edd50929b9011`, terminal revision `12c014eff49cf53c58725683b5aa2efa75d921c3`, clean-entry validation 11/11 with zero browser errors; no requirement impact or new Product review. |
| `/home/autobyteus/workspace/autobyteus-web-prototype/tickets/done/AORG-FLAT-TEAM-STATUS-001` | Focused Product-owned UI/UX supplement, user decision, manifest, `VIS-STATUS-001`–`VIS-STATUS-003`, and validation for mounted Team aggregate status in active-expanded, active-collapsed, and stopped/historical AgentOrg hierarchy states | `Completed and user-approved`; accepted base `12c014eff49cf53c58725683b5aa2efa75d921c3`, approved behavior/integration `dddf1eb1b5facd4827248ffe52bbb211ac170285`, final synchronized revision `1edc1008a55b04d04dd2f07bd9004d53079e09a2`; 9/9 focused and clean-route browser checks passed with zero browser errors, plus typecheck, lint, 15/15 tests, 13/13 boundary checks, and production build. |
| `tickets/in-progress/flat-agent-organization-model/api-e2e-coverage-investigation.md`, `api-e2e-execution-coverage-report.md`, and `api-e2e-evidence/API-REV-001/resumed-crr008/screenshots/ORG-005-direct-agent-conversation.png` | `API-FIND-007` current-production omission and downstream validation context | `Requirement Gap evidence`; current Org Team rows omit the indicator; not implementation completion evidence. |
| `/home/autobyteus/data/memory/agent_teams/software_development_department_d2b93633ad6b4d969e6e0d776dda7721/software_engineering_team_570be46d520142849ac61785be03dca1/api_e2e_engineer_d23a092db04042b69d944416be75d3b9/context_files/ctx_8cd213e66142__image.png` | User-supplied original AgentTeam tree showing Team-row and Agent-row status signals together | `Approved requirement evidence`; placement/affordance basis, not a Product-owned final reference. |
| `tickets/in-progress/flat-agent-organization-model/design-spec.md`, `architecture-design-revision-record.md`, `design-review-report.md`, and `architecture-review-revision-record.md` | Cumulative architecture and independent-review context | Latest completed design is `AD-REV-006@0d71c76ca52c1dab907b81b41702fa4e88fb7538`; latest authoritative review is `ARCH-REV-004@2ae61a11f90cdf5e90786a6ff313af082a21cce8` with `Pass`. `API-FIND-007` is a later Requirement Gap; no architecture artifact changed in this gap round. |
| `/home/autobyteus/workspace/autobyteus-web-prototype/tickets/done/AORG-FLAT-TEAM-001/requirement-impact-findings.md` | Historical Product Design findings `RIF-AORG-001` and `RIF-AORG-002` from user review of `RV-006`/`RV-009` | `Resolved`; authoritative triggers for `RER-011` and `RER-013`, not normative visual references. The rejected `RV-009` selector remains historical evidence only. |

## Assumptions And Resolved Decisions

| ID | Decision | Status / Basis |
| --- | --- | --- |
| DEC-001 | One AgentOrg launch owns one shared runtime/address/lifecycle scope. | Approved; simplest model consistent with current handoff behavior. |
| DEC-002 | AgentOrg has no coordinator. Launch activates the complete Org without an initial recipient; exact Agent/Team focus is required only for a recipient-requiring interaction after launch, and Team focus uses its coordinator. | Approved in `RER-013` from explicit user correction in `RIF-AORG-002`; supersedes pre-launch exact-entry semantics. |
| DEC-003 | The migration population contains no deeply nested configured data. Flat roots remain Teams; one-level organization-like roots convert to AgentOrg; no deep legacy migration/compatibility path is built. | User-confirmed assumption backed by the inventory of 23 root packages and 41 stored trees. |
| DEC-004 | Standalone flat Teams remain reusable and independently launchable. | Approved; preserves existing Team value and progressive composition workflow. |
| DEC-005 | Configured nested Teams are removed. A task Team created inside an Org exists under that Org's execution aggregate at its delegating host scope, but not in configured membership; standalone-Team tasks remain under the standalone Team run. | Clarified by user before approval; preserves lifecycle ownership without restoring configured nesting. |
| DEC-006 | After `AD-REV-001`, the user requested an image-based Product Design prototype and review before independent architecture review. | Satisfied by user-approved Product revision `RV-012`; normative `ui-ux-spec.md` and `VIS-001`–`VIS-020` integrated in `RER-014`. |
| DEC-007 | User-facing `When` is the existing required natural-language guidance for the source Agent, not an executable expression, event subscription, scheduler, or authorization control. | Approved in `RER-012` from current domain/tool contract and `RIF-AORG-001`. |
| DEC-008 | A displayed/authored Handoff is one From/To endpoint pair with one or more ordered When conditions; duplicate effective endpoint pairs remain invalid and additional conditions belong on the existing handoff. | Approved in `RER-012` from the current record/compiler invariant. |
| DEC-009 | Org-owned handoffs and Team-local handoffs have separate editing authorities. Org endpoint choices include mounted Team Agents and direct Team destinations; Team-local endpoint choices include direct Team Agents only. | Approved in `RER-012` from fixed-depth scope/reuse requirements. |
| DEC-010 | Handoff and condition changes are draft operations committed only by an atomic parent-definition save; member changes cannot silently retarget or remove handoffs. | Approved in `RER-012` from validate-before-write and identity-preservation requirements. |
| DEC-011 | AgentOrg activation and communication targeting are separate states: a valid configuration can launch the full Org with no focus, and a user selects the exact communication target from the active workspace only when needed. | Approved in `RER-013` from explicit user rejection of `RV-009`'s separate pre-launch selector. |
| DEC-012 | One Org configuration applies Org-root choices across the scope with Team-placement and Agent-placement overrides by increasing specificity; mounted definitions keep their standalone defaults and are not mutated. Configuration never selects a recipient or changes Team coordinator ingress. | Approved in `RER-013` as the current AgentTeam configuration-first behavior adapted to the approved fixed-depth Org model. |
| DEC-013 | Standalone AgentTeam execution JSON remains native Team V2 at its existing file/path; AgentOrg uses an additional AgentOrg V1 file family with `rootOrg`; no Team V3 or generic root-union file is introduced. Mixed generic APIs use explicit root-kind discrimination while Team-only contracts may remain compatible. | Explicitly approved by the user on 2026-09-01; exact contract recorded in `RER-015` and approval closure in `RER-016`. |
| DEC-014 | The public/private external definition repositories are separate owner work and are never migrated by this ticket. The server publishes one exact target Team Definition V2 and Org Definition V1 admission contract with no normal legacy fallback. | Explicitly clarified and approved by the user; exact policy recorded in `RER-017` and approval closure in `RER-018`. |
| DEC-015 | A Team mounted in an AgentOrg retains one presentation-only five-state aggregate status signal on its Team row, computed from exact descendant Agent-row statuses and visible while collapsed; it does not become an independently active/rooted Team. | Explicit user correction recorded as `API-FIND-007`; exact preserved semantics approved in `RER-020` and confirmed by user-approved focused Product references in `RER-021`. |

## Traceability

| Requirements | Behaviors | Acceptance Criteria | Scenarios / Evidence |
| --- | --- | --- | --- |
| REQ-001–REQ-004 | BEH-001, BEH-002, BEH-004, BEH-006 | AC-001, AC-002 | SCN-001, SCN-002; user direction, current Team launch behavior, and `RIF-AORG-002` |
| REQ-005–REQ-008 | BEH-002, BEH-003 | AC-003, AC-005, AC-015–AC-018 | SCN-001–SCN-003, SCN-008; runtime/handoff sources and `RIF-AORG-001` |
| REQ-009–REQ-011 | BEH-006, BEH-007 | AC-004, AC-006, AC-007 | Real package configs and screenshot |
| REQ-012–REQ-014 | BEH-005, BEH-007, BEH-008, BEH-010 | AC-008, AC-009 | SCN-002, SCN-004; external ownership correction, current Team V2 store/path/schema, and server memory inventory |
| REQ-015–REQ-017 | BEH-006, BEH-008, BEH-009 | AC-010–AC-012, AC-020 | SCN-005, SCN-010; task models, UI, stream/history DTOs, remote draft branch |
| REQ-018 | BEH-001, BEH-006 | AC-013 | SCN-006; explicit user workflow clarification |
| REQ-019 | BEH-001, BEH-003, BEH-004, BEH-006 | AC-014 | SCN-007; explicit user request during `AD-REV-001` review, `RIF-AORG-001`, `RIF-AORG-002`, approved `RV-012`, `ui-ux-spec.md`, `VIS-001`–`VIS-020`, and `user-decision-record.md` |
| REQ-020–REQ-023 | BEH-001, BEH-003, BEH-004, BEH-006 | AC-015–AC-018 | SCN-008; current handoff contract/compiler/tool evidence and direct user feedback in `RIF-AORG-001` |
| REQ-024 | BEH-002, BEH-004, BEH-006 | AC-019 | SCN-009; current AgentTeam configuration hierarchy and explicit configuration-first direction in `RIF-AORG-002` |
| REQ-025 | BEH-005, BEH-006, BEH-008 | AC-020 | SCN-010; current Team-only history/stream contracts and explicit two-family correction |
| REQ-026–REQ-027 | BEH-001, BEH-006, BEH-007, BEH-010 | AC-021, AC-022 | SCN-011; definition codec/provider evidence, external ownership clarification, and Architecture Review recovery |
| REQ-028 | BEH-006, BEH-011 | AC-023 | SCN-012; explicit user `API-FIND-007` correction, original/current browser screenshots, established aggregate status helper/component/contract, focused `ui-ux-spec.md`, and `VIS-STATUS-001`–`VIS-STATUS-003` |

## Downstream Architecture Input

- Preserve fixed semantics: distinct AgentOrg and flat Team, no Org coordinator, direct-to-Org configuration, full-scope launch without initial focus, exact post-launch recipient focus, no fallback, exact addresses, Team coordinator ingress, same-Org handoffs, standalone Teams, explicit data cohorts, and task/configured-nesting distinction.
- Treat `PRE-001`–`PRE-005` and the approved two-family durable runtime contract as authoritative design inputs: retain native Team V2 schema/file/path unchanged for flat Team runs; add AgentOrg V1 at the exact Org file/path; reuse current V2 configured-member/handoff/launch/task shapes; preserve task lineage; and do not create Team V3, a generic root-union file, or a `FlatTeam` subtype.
- Separate source ownership from runtime identity. Design may migrate server-owned definition sources and every server memory runtime package, but it must not write the two external repositories. Normal definition admission is target-version-only; any legacy decoder is confined to server-owned migration.
- Architecture Design owns target modules/classes, internal abstractions, implementation sequencing, compatibility mechanics, lifecycle composition, and removal sequence. The logical JSON keys/versions, execution-tree file names/package roots, and generic root-kind discrimination in `AORG-CONTRACT-001` are product contract inputs rather than architecture placeholders.
- Architecture must verify all definition, execution, persistence, history/memory, task, stream/GraphQL, package, and frontend readers/writers before removing recursive configured-Team paths.
- Native AgentOrg replacement and recursive configured-Team retirement must be delivered in a capability-safe order for server-owned sources and runtime packages. Under approved `RER-018`, incompatible external definitions may be individually unavailable until their owners publish target packages; compatible product/server capability must not be globally stranded.
- The Product Design gate is fully satisfied: user-approved `RV-012` and `VIS-001`–`VIS-020` remain normative for the cumulative experience, while user-approved `AORG-FLAT-TEAM-STATUS-001` and `VIS-STATUS-001`–`VIS-STATUS-003` provide the focused authority for the `API-FIND-007` correction without reopening unrelated behavior.
- Architecture/review recovery was complete through `AD-REV-006@0d71c76ca52c1dab907b81b41702fa4e88fb7538` and authoritative `ARCH-REV-004@2ae61a11f90cdf5e90786a6ff313af082a21cce8` `Pass`. `API-FIND-007` is a later user-authorized Requirement Gap; Architecture Designer made no artifact change in this gap round and awaits approved Product/requirements re-entry.
- The mounted Team signal is an observable presentation aggregate over existing exact Agent status authority. Requirements does not prescribe its component, store, transport adapter, or implementation path. Architecture must preserve the negative boundary: no mounted-Team root registration, persistence authority, lifecycle command, or independent Stop/restore/archive action.
- During the impact check, Architecture Designer must reconcile the approved direct-to-Org configuration/full-scope activation/post-launch focus contract, Org → Team → Agent configuration precedence, no-focus lifecycle, and handoff CRUD/reordering, natural-language condition ordering, owner-separated Org/Team definition APIs, endpoint-option projection, member-impact validation, atomic save/error contracts, responsive/accessibility expectations, and truthful runtime/history hierarchy. Requirements Engineering does not prescribe the target component/API design.

## Readiness Check

- Problem and desired outcome unambiguous: `Yes`
- Current behavior evidence-backed: `Yes`
- Desired/preserved behavior explicit: `Yes`
- Scope/non-goals clear: `Yes`
- Requirements and acceptance criteria stable, testable, and traceable: `Yes — RER-020 adds BEH-011/REQ-028/AC-023/SCN-012 for the approved mounted-Team aggregate status correction; RER-021 adds approved Product evidence without changing semantics`
- Relevant user/system/operational/contract scenarios covered: `Yes`
- Behavior-defining supplements integrated: `Yes — agent-org-contract.md is approved through RER-021; focused Product authority for ORG-CASE-056–058 is integrated from AORG-FLAT-TEAM-STATUS-001`
- Requested Product Design evidence integrated and approved: `Yes — RV-012 remains approved and the focused API-FIND-007 supplement is explicitly user-approved with VIS-STATUS-001–VIS-STATUS-003`
- Data preservation and acceptable loss explicit: `Yes`
- Target architecture avoided: `Yes`
- Existing AgentOrg/AgentTeam behavior approval received: `Yes — explicit 2026-08-31 confirmation recorded in RER-009`
- RER-011 handoff-information clarification approval received: `Yes — recorded in RER-012`
- RIF-AORG-002 launch/configuration/focus reconciliation approval received: `Yes — the user explicitly rejected the selector and stated the desired configuration-first, focus-after-launch model; recorded in RER-013`
- Final Product UI approval received: `Yes — RV-012 and focused AORG-FLAT-TEAM-STATUS-001 are explicitly user-approved`
- Default Product baseline promotion integrated: `Yes — BASELINE-PROMOTION-001 completed at 12c014eff49cf53c58725683b5aa2efa75d921c3; clean normal-entry validation 11/11; no behavior change or review gate`
- RER-015 exact durable contract approval received: `Yes — explicit user approval on 2026-09-01 after confirming that only configured one-level organization-like roots migrate and task-scoped Team lineage remains unchanged`
- RER-017 external repository exclusion received: `Yes — the user explicitly states both external projects are maintained by someone else and are not this ticket's update scope`
- RER-017 exact admission/availability boundary approved: `Yes — explicit user approval on 2026-09-01`
- API-FIND-007 behavior approval received: `Yes — the user explicitly directed restoration of the original mounted-Team status affordance; RER-020 preserves the established aggregate semantics and negative lifecycle boundary`
- Package ready for current downstream route: `Yes — Approved Architecture-Ready`; return the reconciled cumulative package and focused Product authority to Architecture Designer for the `API-FIND-007` impact revision and dynamic review routing.

## Architecture Design Routing Assessment

- Assessment status: `Complete`.
- Assessment owner/date: Requirements Engineer / 2026-09-01.
- Preliminary task size: `Large`.
- Preliminary architectural risk: `High`.
- Structural surfaces reviewed: AgentOrg/AgentTeam definition contracts, graph validation, launch planning, root lifecycle/ownership, canonical routing and handoff compilation, separate Team V2/AgentOrg V1 persistence/restore/package families, memory/history, task delegation, GraphQL/stream discrimination, package discovery/import, and frontend authoring/launch/workspace/history.
- Payload/content surfaces reviewed: 23 external root definitions as read-only topology evidence; one writable data-root Team definition; two built-in application-owned Team definitions; 41 stored TeamRun execution trees at `RER-016` and 43 at the `RER-017` re-probe; handoff snapshots, task execution records, and the current strict V2 JSON contract.
- Structural-impact triggers: `Present` — public/domain contract change, persisted root schema/invariant change, lifecycle/ownership distinction, deterministic data migration, API/transport change, and cross-subsystem structural refactoring.
- Evidence paths: `investigation-notes.md`, `agent-org-contract.md`, current definition/execution source paths listed there, package fixtures, and stored-run inventory.
- Decision rationale: Although the existing Team V2 child/handoff/task topology is reusable and flat Team files now remain unchanged, the overall product change introduces a distinct AgentOrg domain/root and durable file family, removes recursive configured Team composition, requires mixed history/stream discrimination, and changes migration, lifecycle, API, and frontend contracts. It is not safe for bounded direct implementation without architecture design.
- Selected engineering route: `Architecture Designer`; the focused Product evidence gate is satisfied, and `API-FIND-007` now requires an architecture impact revision from the prior `AD-REV-006` / `ARCH-REV-004` authority.
- Current requirements-stage classification: `Approved Architecture-Ready`.
- Direct-route conditions all satisfied: `No` — task is Large/High and has confirmed contract, persistence, lifecycle, migration, ownership, and structural-refactoring impact.
- Architecture design, review, and design-revision artifacts: latest completed `AD-REV-006@0d71c76ca52c1dab907b81b41702fa4e88fb7538`; authoritative `ARCH-REV-004@2ae61a11f90cdf5e90786a6ff313af082a21cce8` `Pass`; no architecture artifact was changed for `API-FIND-007`.
- Downstream re-entry trigger: `Satisfied in RER-021` — Product Prototyper returned user-approved `AORG-FLAT-TEAM-STATUS-001`; Architecture Designer now performs the `API-FIND-007` impact revision and applies dynamic review routing before implementation/API-E2E may claim the finding resolved.

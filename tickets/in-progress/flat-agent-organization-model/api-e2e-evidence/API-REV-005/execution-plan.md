# API-REV-005 Cumulative Execution Plan

## Authority and trigger

- Trigger: `CRR-019 / Pass` over integrated artifact
  `b2c96d6b0eed5ffb3a0eaa503eb7f1cf353e9c9e` (`IR-017`).
- Carried classification: `Large / High`, reviewed route.
- Prior API result: `API-REV-004 / Pass / 98.1%` against the
  pre-integration artifact. It is evidence, not proof of the current artifact.
- New material boundary: latest-base exact task-Agent monitor behavior and
  durability-gated task-Agent event egress.

## Ordered execution

1. Verify artifact, fixture hashes, owned-environment availability, and clean
   initial process/browser state.
2. Run current server task/event/projection, task lifecycle, strict admission,
   persistence, migration, shutdown, MCP, and two-family API cohorts.
3. Run current frontend task-monitor, exact focus/hydration/activity,
   AgentOrg/Team history/stream/recovery/status, responsive, and regression
   cohorts.
4. Build the production server and renderer and run package/build/guard checks.
5. Start an isolated built backend and production renderer with `env -i` and a
   new API-REV-005 data root.
6. Import the deterministic four-Agent/two-Team/two-Org package through the
   production Settings UI.
7. Exercise a fresh standalone Team using real Codex. Create a formal task,
   select its exact task Agent before later work, and prove:
   - durable `TASK_AGENT_ACTIVATED` precedes exact task-Agent Agent events;
   - selected header/row/monitor/activity use one exact AgentRun;
   - retained and later live content advances without reload or refocus;
   - lifecycle and execution statuses remain distinct;
   - configured-parent/task identity stays isolated;
   - settlement performs the approved exact fallback.
8. Exercise authoritative true-empty task-monitor handling using a production
   boundary that proves exact hydration completion before the empty label; do
   not infer a pass from an assignment-only snapshot.
9. Exercise a fresh mixed AgentOrg, mounted-Team task lifecycle/message routing,
   aggregate statuses, and exact direct/Team/nested/task focus.
10. Capture one active two-family GraphQL/persistence snapshot and run the
    corrected 390x844 strip -> drawer -> tree focus journey.
11. Send application-owned SIGTERM with active roots; restart against the same
    isolated data; query mixed inactive history before opening a browser.
12. Continue the exact stopped standalone Team, restore the exact Org through
    supported historical selection, validate provider/task durability, then
    explicitly Stop Agent Org and confirm root isolation.
13. Re-run strict negatives, migration/startup, automatic recovery, fixture
    immutability, final SIGTERM, port/tab/process cleanup, and diff hygiene.
14. Reconcile the ledger and update the canonical investigation, execution
    report, and `API-REV-005` revision entry.

## Evidence discipline

Every independently meaningful case is started and completed in the canonical
ledger before the next case. Browser screenshots support DOM/API/process
evidence; they never substitute for it. Any setup-only failure is isolated and
rerun before product attribution.

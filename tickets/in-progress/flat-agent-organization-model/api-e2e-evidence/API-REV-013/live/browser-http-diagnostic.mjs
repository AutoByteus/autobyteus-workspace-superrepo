import fs from 'node:fs/promises';import pw from '../../../../../../autobyteus-web/node_modules/playwright-core/index.js';const b=await pw.chromium.connectOverCDP('http://127.0.0.1:9222');const p=b.contexts().flatMap(c=>c.pages()).find(p=>p.url().includes('127.0.0.1:3592'));if(!p)throw new Error('missing tab');const bad=[];const errors=[];p.on('response',r=>{if(r.status()>=400)bad.push({url:r.url(),status:r.status(),requestType:r.request().resourceType()})});p.on('console',m=>{if(m.type()==='error')errors.push({text:m.text(),location:m.location()})});await p.reload({waitUntil:'networkidle',timeout:30000});await p.waitForTimeout(1000);const result={at:new Date().toISOString(),url:p.url(),bad,errors,bodyHasWorkspaces:(await p.locator('body').innerText()).includes('Workspaces')};await fs.writeFile(new URL('browser-http-diagnostic.json',import.meta.url),JSON.stringify(result,null,2)+'\n');console.log(JSON.stringify(result,null,2));await b.close();
